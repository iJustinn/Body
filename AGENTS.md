# Body Project Instructions

- For every code-changing task in this repository, use the project-local `body-rtk-tdd` skill.
- Follow Red-Green-Refactor for bug fixes, implementation work, refactors, model changes, formatting changes, HealthKit/workout logic, widget logic, and test updates.
- If a strict failing-test-first step is not practical for a SwiftUI layout-only change, state why and run the strongest relevant `xcodebuild` build or test gate before finishing.
- Keep changes scoped, preserve unrelated local work, and report the RTK path used in the final response.
