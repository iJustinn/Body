# Refresh Optimization Plan 03

Data-path refinement: correctness first, then durable reuse, freshness scheduling, and only the structural changes those require.

> **Status (2026-09-06): Implementation Phases 1–4 are committed on 1.1.0 build 8. Phase 5 physical mutation, relaunch and completed dependent repair were verified on the approved iPadPro reference device; the user confirmed correct display/refresh and code review without must-fix findings. Production journal activation is approved and enabled; post-activation app build, full app tests, watch tests and ordinary-build iPad refresh checks passed. Phase 5 implementation/activation is complete. Query-derived refreshes remain authoritative. Complete matched cold/warm/first-usable baselines remain outstanding, and no performance improvement is claimed. Phases 6 and 7 remain explicitly deferred. No new commits are authorized.**
> Reviewed baseline: `ee65d15`; working tree was clean at entry. References below are to this revision, not RP-01/RP-02's line numbers. `Store` means `Body/Services/HealthKitWorkoutStore.swift`; `Engine` means `Body/Services/HealthKitFetchEngine.swift`.
> Method: CodeGraph first for the named orchestration, fetch, persistence, compute, consumer, and test seams. Several responses returned unrelated symbols or omitted the requested bodies; targeted source reads filled those specific gaps. Findings are static source proofs or explicitly labeled inferences, not claims of reproduced device failures.
> Performance: partial iPad measurements are recorded in the Phase 5 execution notes. They are not a complete baseline and are not comparable to prior iPhone timings. No speed estimate from RP-02 is carried forward. See §6 and the approved sequencing amendment.
> Adversarial review: completed by a separate read-only agent on 2026-09-04. Verified corrections are folded in and listed in §8. The reviewer made no file changes.

## 1. Findings, ranked by correctness and value

### F1 — HIGH: context changes can preserve old-source trends and stamp them as current

**Evidence.** `Store:3693` (`updateHealthDataSource`) changes the stored selection and engine selection before waiting for the running refresh; the default-source and group/combine paths have the same ordering (`Store:3622`, `:3603`, `:3444`). The corrective source refresh strips intraday samples, not the corresponding daily trends. In `fetchDashboardSnapshotProgressively` (`Store:5085`), the context signature gates summary fallback and the short trend window, but `cachedTrendsAtStart` is passed to `fetchHealthTrends` even on a signature mismatch. `Engine:3803` resolves a failed daily leaf with its cached series unconditionally. Finally, `updateHealthDashboardSnapshot` captures inputs before detached compute (`Store:5897`) but checks only `cacheEpoch` and the refresh generation at `:5983`; `:5993` stamps the *current* context signature after the await.

**Failure A, no concurrency required:** cached HRV daily trend from source A → select B → B trend query fails → engine returns A's cached trend → final publish stamps B → A's trend can feed readiness and survive on disk as B. The summary has a mismatch gate; the trend does not. A later successful full-window B fetch repairs it, but repeated failures do not.

**Failure B, interleaving:** A's queries or detached compute are suspended → source/group/permission changes → A completes → the unchanged refresh/cache generations admit A's payload and the new signature. The queued corrective refresh helps only if it subsequently succeeds. Disabling a permission can also race the detached filter captured before the disable (`updateHealthPermission`, `Store:3190`); the disable path has no corrective full refresh.

**Confidence:** high for the source-failure fallback; high for the missing commit-context guard, with the exact interleaving to reproduce using suspended fetch/compute barriers. This is not a claim that every source change fails.

**P1, proposed change.** Introduce a small immutable fetch/compute context covering query-affecting selections, effective source/group resolution, permission selection, sleep parsing preferences, and calendar aggregation identity, plus compute-affecting preferences such as the sleep goal, calculator version, and effective layout/Radar enablement. Fetch and compute compatibility may be separate projections so a sleep-goal change does not refetch raw HR. Capture the appropriate context before dispatch, carry it with results, and require equality at every progressive/final publication and persistence admission. Advance the revision synchronously on context changes, before the first await. Keep `cacheEpoch` for destructive cache resets and the refresh generation for abandonment; they are different fences. Filter cached *daily and intraday* fallback by its captured scope before dispatch. On a source mismatch, use no old-source fallback for affected leaves. Do not stamp an entire snapshot as current when some carried fields have another scope. Coalesce a corrective refresh under the latest context without delaying the settings interaction.

**Cost/risk.** Medium, spans entry points and publication guards. Overbroad invalidation can blank unrelated cards; use per-kind compatibility for cached values and one revision to reject in-flight work. Do not solve this by serializing the fast summary behind trends or by moving the whole store to a new architecture.

**Verification.** Extend `HealthKitFetchEngineFailureSemanticsTests`, source-resolution tests, and store integration tests with A→B plus B failure, group membership edit during query, permission disable during detached compute, and timeout followed by a new-context refresh. Check Home, persisted reload, widget input, watch input, and readiness inputs. A stale result must neither publish nor advance freshness. Source display-name-only changes must remain harmless.

### F2 — HIGH: ring progress and refresh freshness can outrun durable payloads

**Evidence.** `landActivityRingBackfillChunk` (`Store:5331`) applies a chunk, calls `persistActivityRingHistory`, then writes the resume point to UserDefaults (`:5345`). `persistActivityRingHistory` (`Store:5400`) only enqueues the snapshot save. The terminal backfill state is also saved before the final history save (`Store:5291`). Dashboard success is persisted by `markRefreshSucceeded` (`Store:6427`, success branch at `:6486`) before the deferred dashboard write at the end of `refreshRecentMonths` (`Store:4939`). `HealthDashboardSnapshotStore.save` (`Body/Services/HealthDashboardSnapshotStore.swift:241`) returns `Bool`, conflating unchanged data with a failed write.

**Failure:** ring chunk covering an old year lands → its checkpoint advances → process exits before the queue writes, or the atomic write fails → relaunch reads an older payload with a newer checkpoint → the skipped year is not queried again. A prematurely durable `completed` marker is worse: normal later refreshes only query recent rings. Separately, a successful dashboard fetch followed by a failed/deferred disk save can leave a fresh five-minute timestamp beside an old dashboard on relaunch.

**Confidence:** high for ordering and unchecked write failure; process-kill timing is an inferred reproducer. Per-file atomic writes are intact. The defect is metadata/payload consistency, not file truncation.

**P2, proposed change.** Put ring checkpoint/completion state and dashboard freshness metadata in the same versioned persisted dashboard envelope as the payload they describe. Migrate legacy UserDefaults progress conservatively: preserve existing history but restart an unprovable walk, with bounded queries. A save outcome must distinguish written, unchanged-and-valid, and failed; only the first two can acknowledge durability. Keep live success separate from durable success so progressive UI publication does not wait for disk. Fold the secondary-selection stamp into the envelope as well; it currently lives separately from the trends it qualifies. Keep the day-sample sidecar separate and independently scoped; this proposal does not require a cross-file transaction for every raw sample.

Report main-envelope and sidecar outcomes separately. The main file is written before the sidecar (`HealthDashboardSnapshotStore.swift:290`); main success must not clear raw-sample dirty coverage or acknowledge an authoritative-empty repair when the sidecar failed. A later sidecar retry may complete that repair without refetching a successfully captured payload, provided its context and input revision still match. Test both partial-write directions and reload behavior. Main-envelope completeness must not claim raw inputs were durably validated merely because a derived display value was saved.

**Invalidation key:** envelope schema, captured context revision/signatures, payload revision, and ring coverage/checkpoint carried together. Missing/legacy metadata means unverified coverage, never completed by assumption.

**Repair:** a legacy or inconsistent marker causes a resumable reconciliation without deleting existing months. A failed write retains the prior envelope and retries. Clear Cache remains an explicit complete rebuild, never the only way to recover skipped chunks.

**Cost of being wrong:** missing historical rings and false cold-start freshness; this can also defer correct derived-score inputs. Do not use a durable timestamp to authorize notifications.

**Cost/risk/verification.** Medium. Migration must not discard history or turn a sidecar failure into a destructive empty save. Inject encode/write failure and pause the persist queue between chunk arrival and write; reload at each boundary. Verify checkpoint never exceeds committed coverage, terminal completion is atomic with the final history, unchanged saves can acknowledge valid coverage, and a failed dashboard write does not arm cold-start freshness. Extend dashboard persistence, day-sample, ring chunk, and save-dedupe tests.

### F3 — HIGH value: age and loaded-state are not mutation detection

Three distinct artifacts share this problem; they need different repair boundaries.

| Artifact | Evidence | Concrete failure and existing repair |
| --- | --- | --- |
| Intraday sample sidecar | `HealthKitFetchEngine+IntradaySamples.swift:63,73,91`; `Engine:3957,3999` | Delete or backfill a sample seven days ago while the latest cached point is today. The 48-hour overlap never visits that day, so detail pull still shows the old sample or misses the new one. Daily statistics may be correct at the same time. Clear Cache repairs it; hourly cumulative paths have separate full-window handling and are not all subject to this exact failure. |
| Effort ledger | `Engine:333,356,420,477,2739`; `WorkoutEffortLedgerStore.swift:21` | Edit an effort score on a five-month-old workout. The 408-day training-load workout read sees the workout, but the aged UUID's cached effort is reused across launches. Home pull only clears its requested months. Training Load detail pull performs a full effort clear; pulling that workout's month or Clear Cache also repairs it. Merely opening an already-loaded month does not prove repair. |
| Month snapshots and all-time records | `Store:4351,5555,5596`; `HealthKitWorkoutStore+Records.swift:68,105,207` | Delete the record-holder from a previously loaded old month. `loadedMonthKeys` prevents another lazy read; the one-time completed record baseline does not rescan. The stale PR can remain until the affected month is explicitly refreshed or the cache is rebuilt. A fresh authoritative month *does* remove absent record UUIDs. |

**Confidence:** high. These are explicit reuse policies, not speculative query costs. The effort ledger already has a full reconcile path; the plan must not claim reinstall is required.

**Additional verified durability defect:** `HealthDashboardSnapshotStore.swift:310` preserves a populated sidecar when an incoming all-empty payload has matching scope. That protects pre-hydration saves but cannot distinguish them from an authoritative successful read that removed the final samples. In the latter case memory becomes empty while disk retains deleted samples, which can hydrate on relaunch. A full raw repair must fix this distinction, not merely widen its query.

**P3, proposed immediate repair.** Give explicit metric refresh a full retained-window reconciliation for sample-based intraday data, replacing only successfully queried coverage. Retain the 48-hour overlap for passive refresh. Add bounded freshness to existing effort entries and loaded workout months: store validation time independently of `generatedAt` (unchanged-content saves intentionally preserve that timestamp). Proposed conservative initial policy: aged effort entries revalidate after 24 hours, visible loaded months revalidate on resume after five minutes; clocks moving backward mean stale. Failures retain values without advancing validation. For all-time records and old rings, schedule a rolling bounded historical repair sweep instead of treating a completed baseline as proof of immutability; checkpoint it using P2's durability rule. Foreground work still takes priority.

Give sidecar persistence explicit per-series write intent/authoritative coverage: not loaded, failed, or successful (including empty). Successful empty coverage must clear those persisted samples; unhydrated or failed coverage must preserve them. This also permits safe scoped saves without interpreting a default empty series as a deletion. Test deleting the final samples, saving, and relaunching, as well as a pre-hydration save that must retain history.

**Invalidation key:** per-artifact context/schema plus last successful authoritative coverage check; for effort, UUID and workout dates plus validation date. This is bounded staleness, not exact mutation detection. P6 later adds dirty generations/anchors. A successfully empty queried interval must remove absent cached values; a failed read must not.

**Repair:** metric pull reconciles its retained raw window; Training Load pull reconciles all effort inputs; month pull reconciles that month's membership and record contributions. Rolling history repair must revisit already-scanned history. Clear Cache remains the broad fallback. Do not clear unrelated metrics or delete original HealthKit samples.

**Cost of being wrong:** intraday disagreement can affect stress baselines, effort staleness affects training load/readiness, and an old workout can misstate records. Ring counts/PR badges are display errors; cached score inputs must not be promoted to freshly validated data for downstream consumers.

**Cost/risk.** Medium. Reconciliation deliberately does more work in repair paths; no speed claim. The 24-hour policy bounds effort staleness only while the app is active often enough to run it. The historical sweep has no promised completion time while iOS denies execution. Track successful coverage, not just visits. Do not re-run full ten-year scans at each launch.

**Verification.** Fake-store add/delete/late-arrival scripts at 47 hours, 49 hours, seven days, five months, and a prior year; same UUID with changed effort; failed and successful-empty reads; relaunch; clock rollback; source change. Assert explicit pulls repair within their stated scope, passive policies expire, and record deletion reconciliation removes only absent UUIDs in the queried interval.

### F4 — MEDIUM: source discovery can stay stale for the process lifetime

**Evidence.** `HealthKitFetchEngine+SourceOptions.swift:79` skips discovery once permission raw value matches and the map is nonempty. `Engine:324` clears this cache; combine/group membership edits do so, but ordinary Home/metric pulls do not. `discoverHealthSources` (`+SourceOptions.swift:150`) also skips already-present kinds.

**Failure:** discover a combined-name source containing device A → a second same-name device B begins writing in the same app process → the selected group still resolves through A's old map → B's samples are omitted. All Sources queries do not have this particular predicate problem, but the source picker can still miss B. Restart or a cache/group reset repairs discovery.

**P4, proposed change.** Give discovery an explicit force/dirty/expiry policy, invoked by user-initiated source/metric repair and after the observed type changes. Refresh group membership before dependent leaf queries. Use a conservative 24-hour successful-discovery expiry as a fallback, independent of Body's permission string. Failures retain prior maps but keep discovery dirty. A changed resolved membership must invalidate matching artifacts even when the user's selected ID is unchanged (P1).

**Cost/risk/verification.** Small to medium, bounded extra source queries. No additional persisted source cache is proposed: it would require the same external-change detection anyway. Script changing source lists with stable preferences, partial discovery failure, deleted last source, and a rename-only custom-group edit. Confirm resolved source sets, dependent trends, and watch source expectations change together.

### F5 — MEDIUM, high confidence in key omission: daily aggregate cache identity omits time zone

**Evidence.** `currentPrimarySummarySignature` (`Store:5465`) has sources, permissions, combine/group and sleep parsing flags, but no calendar/time zone. The sidecar signature (`Store:5522`; `BodyMetricsKit/HealthTrend.swift:1240`) similarly has no aggregation zone. Daily statistics receive the current calendar (`Engine:1351`), while phase-one merge retains pre-window points (`Engine:896`). `Calendar.bodyGregorian` is rebuilt when the current zone ID changes (`BodyMetricsKit/WorkoutMonthSnapshot.swift:397`). Workout grouping already has an instant-scoped time-zone ledger (`Store:5657`; `BodyTimeZoneLedger.swift:99`), which must be preserved.

**Failure:** cache daily HRV buckets in New York → move to Los Angeles without changing selections → phase one merges newly bucketed recent days with old-zone historical buckets → chart/day matching and readiness baselines temporarily mix aggregation contexts. If phase two fails, the mixture lasts longer. A full successful year reconciliation repairs daily aggregates. This is an inference from bucket identity and merge logic, not a claim that the existing travel-day workout or sleep metadata logic is broken.

**P5, proposed change.** Add calendar identifier/time-zone ID and aggregation-policy version to *calendar-bucketed* artifact identity. On a zone change, retain instant-valued raw samples, invalidate/rebuild affected daily/hourly aggregate coverage, and recompute current derived state before labeling it current. Do not globally discard the workout time-zone ledger or move historical workout date keys to today's zone. Source metadata and sleep wake-day policies remain authoritative in their existing domains. Day rollover should invalidate today's summaries independently of a five-minute elapsed-time TTL.

Do not append the aggregation-zone key to frozen readiness/Radar observation reset signatures. Those signatures trigger wholesale record clearing (`HealthSummarySnapshot.swift:788,1043`), which is a different product behavior. Preserve frozen observations and their original day attribution; the new zone key governs recomputable aggregates and current calculation admission. A change to frozen-observation attribution itself would require a separately reviewed plan.

**Invalidation key:** captured calendar/time zone/aggregation version, plus ordinary source and input revisions; raw timestamps do not become invalid merely because the display zone changes.

**Repair:** scope mismatch queues full aggregate reconciliation with existing raw data reused where mathematically equivalent; explicit metric pull forces that reconciliation; failed reads remain unverified. No history deletion.

**Cost of being wrong:** misbucketed charts and changed derived baselines, higher consequence than formatting. Cost/risk medium: re-bucketing raw instants is cheap relative to fetching, but a statistics-only historical cache cannot be re-bucketed exactly without querying HealthKit.

**Verification.** NY↔LA and UTC±date-line transitions, DST 23/25-hour days, a change while phase one is suspended, midnight inside the five-minute TTL, and a failed phase-two fetch. Retain existing `BodyTimeZoneLedgerTests` and workout travel tests; verify daily trends separately from historical workout attribution.

### F6 — MEDIUM: one unrelated trend failure blocks all historical reconciliation

**Evidence.** `loadFullTrendWindow` (`Store:3043`) requires `!result.hadQueryFailure` before applying any full-window series. The aggregate failure includes secondary leaves and sleep-vital hydration (`Engine:3802,3840`). Phase one still merges older cached primary data. `mergeWindowedTrend` retains every point before the merge boundary, with no separate oldest-retained cutoff (`Engine:904`).

**Failure:** old primary steps changed → recent phase one cannot see the change → full steps query succeeds but an unrelated comparison source repeatedly fails → the whole phase-two result is discarded → old steps never reconcile through this path. Repeated short-window merges can also retain points beyond the nominal full-window horizon until a successful replacement occurs. A same-context explicit full metric pull repairs that metric today.

**P6a, proposed change.** Carry per-leaf outcome and successful coverage through the trend result, so phase two replaces successful compatible leaves while leaving failed ones dirty. Preserve live intraday/frozen fields as `applyingFullWindowTrendSeries` already does. Bound retained *rolling* trends to their documented retention interval; this must not prune recorded observations or unrelated ring/workout history. Require context and data-revision equality at admission, not only equality of `lastSuccessfulRefreshDate`: a failed or partial concurrent refresh can change data without advancing that timestamp.

**Cost/risk/verification.** Medium. Avoid teaching every consumer a new result algebra: metadata belongs at fetch/merge/admission boundaries. Test successful primary plus failed secondary, sleep-vital partial failure, a concurrent partial refresh, and data older than the nominal window. Assert early summary paint and per-month streaming remain, corrected primaries persist, failed leaves retain their own compatible cache, and no whole-snapshot replacement discards concurrent stress inputs.

### F7 — MEDIUM: background warning evaluation holds stale state across awaits

**Evidence.** `MetricWarningBackgroundEvaluator.swift:66` checks the master switch, selected kinds, and foreground status before awaited settings/query work. At `:103` it holds a local ledger across `postNotification`; `seed(kinds:)` can modify the actor's persisted ledger while the notification add suspends. The evaluator writes its older ledger copy afterward (`:118`). Stable kind/day request IDs (`:214`) reduce duplicate request identity, but do not prevent lost ledger updates. The previously reported unenforceable deadline is fixed: `fetchWarnings` now uses `OneShotDeadlineRace` (`:159`).

**Failure:** background evaluation starts enabled → user disables notifications/changes thresholds or sources while fetch is pending → old event is still submitted. Or foreground seeds kind B while background posts kind A → background saves its pre-seed ledger → B's suppression record is lost. Duplicate alert behavior depends on notification-center delivery timing; the lost update itself follows from actor reentrancy.

**P7, proposed change.** Capture an evaluation settings/context revision and recheck it, cancellation, enabled kinds, and foreground status immediately before each add. Reload and merge the ledger after the awaited add rather than writing a stale copy; reserve an in-flight kind/day on the actor if overlapping evaluations are permitted. A notification already handed to the system cannot be recalled by a later pre-submit guard; define the guarantee at submission. Keep warnings on fresh event queries. Current warnings are low/high HR and low oxygen, not readiness notifications.

**Cost/risk/verification.** Small, one evaluator and a notification-submission test seam. Suspend before submission and during add; toggle master/kind/threshold/source, seed another kind, cancel, and start a second evaluation. Assert stale events are not submitted after a pre-submit change and unrelated ledger entries survive. Do not mark notified on failed delivery.

## 2. Persist more, load less: verdict and bounded expansion

The app already persists the expensive reusable outputs: dashboard/daily trends, intraday sidecar, multiple workout months, settled workout details, effort outcomes, record contributions, stress history, and frozen readiness/Radar observations. The immediate opportunity is making those caches *verifiably reusable*. A new blob without invalidation would duplicate the current weaknesses.

### P6b — Reopen anchors as mutation detection; retain statistics aggregation

RP-02's rejection was appropriate for replacing statistics queries with client aggregation to save seconds. It is too broad for this pass. Anchors can identify changes that invalidate a cached statistical bucket without replacing `HKStatisticsCollectionQuery` as its authoritative calculator. Apple documents anchored additions/deletions and observer notifications as separate mechanisms: [anchored queries](https://developer.apple.com/documentation/healthkit/executing-anchored-object-queries), [observer queries](https://developer.apple.com/documentation/healthkit/executing-observer-queries).

**Proposed first scope:** a bounded workout change journal and canonical workout input cache; no high-frequency raw HR mirror. Consume additions/deletions, dirty affected workout months, effort outcomes, record contributions, and dependent training-load/readiness inputs. A deletion supplies identity; preserve UUID→interval metadata so it can invalidate the old interval. If that mapping is absent, conservatively dirty the retained dependency window and the record baseline. Do not assume a workout anchor observes edits to related effort or HR samples: those types need their own change signals or P3's revalidation policy. Keep the effort TTL/full-pull repair even after workout anchors land.

**Anchor scope:** query type, fixed predicate definition/version, store-local installation identity, and schema. Do not reuse an anchor against a moving `now − 408 days` predicate. Start with a fixed lower-bound journal epoch; advancing retention is a separate prune/rebootstrap operation. Bound batches and commit between them. A schema/scope change or rejected/unreadable anchor triggers bootstrap; an empty incremental result is not proof that read authorization is still granted. HealthKit does not expose ordinary read permission as a reliable yes/no status: [authorization guidance](https://developer.apple.com/documentation/healthkit/authorizing-access-to-health-data).

**Commit rule:** write delta effects and new anchor in one atomic journal envelope, or persist the dirty work and anchor atomically before acknowledging the batch. Never advance the anchor first. Replayed UUID updates/deletes must be idempotent. Mark an artifact clean only after its recomputation and durable write succeed under the same revision. On suspension/deadline, retain the durable dirty work and retry; a partially applied batch must not become a complete cache.

**Invalidation key:** journal schema/query scope and anchor generation; canonical workout UUID revisions; independent effort validation revision; permissions/source/calendar/calculator versions for derived payloads. No content hash computed only from yesterday's cache can detect a HealthKit edit.

**Repair:** discard the cursor, not user history, then bootstrap into a temporary generation and reconcile covered intervals. Unknown deletions dirty the full relevant window. Explicit Training Load/month pull bypasses delta trust. Clear Cache deletes the rebuildable journal and restarts it. Keep a periodic full reconciliation because permission changes and interrupted/unsupported delivery cannot be inferred from an empty delta alone.

**Cost of being wrong:** missed deletion could retain a record or incorrect training load and readiness. Thus new journal validity cannot by itself advance watch `dataThrough`; all input dependencies must have validated coverage. No cached journal authorizes warning notifications or automatic effort writes.

**Cost/risk/verification.** High relative to the earlier fixes: new persistent protocol and HealthKit seam, not a small query swap. Extend `BodyHealthQuerying`/`FakeHealthStore` with scriptable anchored batches, deletions, anchor failures, and cancellation; do not fake only `execute(HKQuery)` counts. Test first bootstrap, multi-batch replay, delete-before-mapping, kill after every commit boundary, invalid anchor, denied/empty reads, and mutation during reconciliation. Device verification is mandatory. Implement only after P1/P2/P3/P6a and the baseline gate. If the device experiment cannot establish safe mutation coverage, stop and revise this step with the user; do not silently substitute UUID union merging.

### P8 — Conditional durable training-load inputs, not another derived-score cache

`HealthKitFetchEngine+TrainingLoad.swift:34,81,104,130` already shares one in-flight workout fetch across summary, series, and watch daily-load seed. It still fetches a 408-day window each refresh. Persisted month snapshots alone do not establish complete coverage for that window, and their downsampled/detail-enriched shape is not a replacement for canonical compute inputs.

**Proposal, conditional on P6b correctness and §6 measurement:** retain the minimal canonical workout fields needed by `TrainingLoadCalculator`, with independently validated effort outcomes and an explicit covered interval. Reuse that cache to derive summary, series, and watch seed when complete and clean. Do not persist another readiness/stress result merely to avoid an unmeasured recompute. Raw workout objects and routes are unnecessary for this artifact.

**Invalidation key:** canonical workout journal revision, effort revision/validation expiry, complete 408-day coverage, calculator version, calendar and permission context. New current-day boundaries advance the requested coverage. Any dirty dependency prevents a claim of fresh reuse.

**Repair:** explicit Training Load refresh re-fetches/revalidates its complete input window; missing/corrupt/incompatible coverage falls back to the existing shared query and replaces only covered inputs after success. Bootstrap does not delete usable month history. Periodic full audit checks delta correctness.

**Cost of being wrong:** stale training load propagates to readiness and the watch compute seed, so a stale display may remain available with its old watermark, but cannot be advertised as freshly validated or used to bypass write-time checks.

**Cost/risk/verification.** Medium after P6b; otherwise high and not justified. Dual-run existing query-derived versus cache-derived daily loads on identical fixtures and on-device data, including removed/re-rated workouts, complete empty days, midnight, and no workout permission. Require identical inputs/results and measured reduction in queries/launch cost before switching the production path. This is a conditional follow-on, not approval to infer completeness from six month files.

### Persistence proposals deliberately not selected

- **More dense workout HR/route data:** the detail cache already persists settled routes, splits, metric series and recovery; dense HR is session-only (`BodyWorkoutDetailCacheStore.swift:72`). No measured reason to add another large sidecar. Its independent sample mutation and permission invalidation costs are unresolved for a net benefit.
- **Persisted source discovery maps:** cheap correctness invalidation must precede this; current session latch is already too strong (F4).
- **Replace sleep summary with history's latest day:** still not equivalent. Summary and historical sleep have different vital/session and failure semantics. Keep their independent early publication; no coupling to the slow bucket.
- **Automatically rewrite frozen morning observations:** intentionally not selected. Readiness overlay (`HealthSummarySnapshot.swift:771`) and Radar freeze (`BodyRadarCalculator.swift:72,177`) are product contracts, covered by `TestPlan.md` M55/M64/M358. A changed historical HealthKit sample can disagree with an observation recorded that morning without violating that contract. Source/context changes already reset these records. Preserve that distinction in tests and repair UI; do not silently turn the feature into retrospectively recalculated history. Historical stress, which is recomputed data rather than a once-per-morning verdict, does need dirty-window repair through P3/P6b. Reconstructing discarded morning observations after Clear Cache is inherently impossible; never describe Clear Cache as a lossless restore of those observations.

## 3. Freshness and when work happens

### P9 — Observer-driven invalidation plus bounded refresh scheduling

**Current state.** No phone observer/anchored/background-delivery implementation was found in `Body` or `BodyWatchSnapshotKit`. `BodyBackgroundRefreshScheduler.swift:51,71` schedules a 30-minute-earliest task only when warning notifications are enabled and executes `MetricWarningBackgroundEvaluator`; it does not refresh dashboard/widget snapshots. Its one-shot completion and expiration handling are useful existing mechanisms. `syncWhenAppBecomesActive` (`Store:4205`) uses 60-second debounce / 300-second dashboard freshness and current-month-only warm refresh.

**Failure/freshness gap:** a new sleep/workout/sample arrives while Body is backgrounded → warning BG task, even if it runs, does not update Home/widget data → user resumes inside the TTL and receives only the workout path, or no refresh inside 60 seconds. No signal overrides the time gate. This is a policy limitation, not a claim of a missed OS delivery that Body registered for.

**Proposal.** Install observers at app launch, initially for the types covered by the reconciliation design. Their job is to persist coalesced dirty generations and request bounded work; they are not a second dashboard store. Foreground dirty kinds/context/day changes override the debounce/TTL. A refresh that was already running must leave a pending follow-up if it did not cover the newest dirty generation. Keep user pull as an explicit repair intent. A normal clean resume keeps the current policy until measurement supports changing it.

For background delivery, enable only supported/needed types and use the required entitlement/lifecycle setup. Route bounded reconciliation through a single owner, using the background query pool and a task budget below the OS expiration window. Do not run the foreground 120-second pipeline blindly in a background callback. Persist dirty work if the device is locked, a query fails, or execution expires. Complete each observer delivery exactly once after its change has been safely accounted for, including error paths, without waiting for unrelated history walks. Do not claim delivery is immediate or guaranteed. Apple explicitly requires completion handling and real-device testing for background queries: [observer lifecycle](https://developer.apple.com/documentation/healthkit/executing-observer-queries).

Keep warning scheduling independent: disabling notifications must not disable data invalidation. If a BGAppRefresh reconciliation fallback is added, give it an explicit separate responsibility from warning evaluation; do not make notification opt-in a hidden dashboard-freshness toggle. Registration occurs at launch; scheduling remains best-effort. Reuse the existing scheduler's completion pattern, not its warning-only policy.

**Invalidation key for persisted dirty work:** sample type/domain plus monotonically increasing dirty generation and context revision; reconciliation records the highest generation actually covered. Dirty journal schema belongs to P6b's envelope, or a small independently atomic queue if anchors are not yet enabled.

**Repair:** launch/resume drains dirty work even if no background callback ran; explicit pull reconciles the requested domain; periodic coverage audit repairs missed/unknown changes. Failed background execution never marks clean.

**Cost of being wrong:** falsely clearing dirty state leaves misleading scores or stale widgets. Extra dirty events cost queries/battery, so favor conservative retry and coalescing over suppressing uncertain work. Warning submission still uses P7's fresh-query path.

**Cost/risk/verification.** Medium to high: lifecycle/reentrancy and energy work, not a speed tweak. Test duplicate/coalesced events, change during refresh, failure/cancellation/locked reads, opt-out, relaunch with dirty work, and callback completion counts. On device: sleep sync and workout arrival with app backgrounded, notification toggle off, Background App Refresh disabled, and foreground re-entry. Record query counts and energy behavior; retain the two pools and early Home-card paint.

## 4. Verified green, no action needed in these mechanisms

“Green” here means inspected source satisfies the named property; it does **not** mean the full test suite or a device benchmark was run in this plan-only pass.

- **Deadline abandonment:** `Store:590` races without joining a hung loser, retires the refresh generation, resets the engine anchor, awaits dashboard recovery persistence, enqueues landed workout months, and republishes watch data. Keep this shape. The 120-second constant remains at `Store:559`. Persistence consistency still needs F2; the deadline is not proof that disk or every background task finishes within 120 seconds.
- **Bounded ring queries:** `HealthKitFetchEngine+ActivityRings.swift:402,520` uses a stoppable query and 20-second timeout. Keep bounded chunks, permission/epoch gates, and `replacingLoadedMonths` (`Store:5384`); replace only queried months, never the entire ring history with the recent window.
- **Two query pools:** `HealthKitQueryBudget.swift:88` defines interactive 10/background 4; task-local routing and query wrappers release permits in `defer`. No proposal to retune limits from RP-02 estimates. Semaphore waiters are not themselves cancellable; this is not a proof that arbitrary background walks cannot stall.
- **Fetch tiers:** `Engine:1015` defaults leaves to `fullOnly`. The three `inputCapable` sites remain summary sleep (`:3356`), history sleep (`:3572`), and HRV trend pair (`:3647`), with existing sleep/history clamps. `BodyDashboardFetchSelection` (`BodyAppearancePreference.swift:958`) distinguishes readiness, stress, and Radar dependencies. New observer/repair work must not broaden a stress-only foreground layout's payload by accident.
- **Progressive publication:** independent summary/trend buckets (`Store:5127`) and out-of-band rings preserve the three visible stages; month results stream at `Store:5652`. Do not restore a single barrier for all three or make summary depend on history completion.
- **Atomic files and byte dedupe:** dashboard store encodes before atomic write and scopes its sidecar; workout store (`BodyShared/Services/WorkoutSnapshotStore.swift:130`) and widget store (`HealthWidgetSnapshotStore.swift:42`) likewise encode first, sort keys, compare bytes, and preserve the old file on encode failure. Workout/widget stores also ignore timestamp-only differences. F2 concerns separate metadata, not these per-file guarantees.
- **Cold restore and scoped hydration:** dashboard payload/context use one decode (`HealthDashboardSnapshotStore.loadOrEmptyWithContext`); intraday hydration is off-main and scope filtered (`Store:2508`, `HealthTrend.swift:1408`), with fill-only behavior. Unknown sidecar schema and disabled comparisons do not hydrate. Do not reintroduce the old double decode or inline readiness rebuild. The populated-sidecar preservation guard is not globally green: F3 covers its authoritative-empty failure.
- **Existing effort and detail safety:** effort clear/hydration generations and FIFO disk deletion (`Engine:333,420`) prevent pre-clear effort writes returning; scoped pull repair and Training Load full repair exist. Detail hydration checks live app permissions and bypasses persisted seeding after background transitions (`Store:2291`); positive-only persistence and failed-read handling are worth retaining. These do not establish retroactive-mutation detection or close every in-flight permission race (P1).
- **Record month reconciliation:** `HealthKitWorkoutStore+Records.swift:68` removes absent UUIDs from the refreshed interval and upserts arrivals. The bug is not an append-only *month* merge; it is never revisiting old coverage after baseline completion.
- **Shared training-load fetch:** one memoized task feeds summary, series, and watch loads, and anchor reset cancels it (`+TrainingLoad.swift:34`, `Engine:308`). No duplicate workout fetch should be “removed” here.
- **Watch ordering and freshness distinctions:** capture sequence admission (`WatchConnectivityPublisher.swift:71,175`) plus durable publisher epoch/revision prevents older captures winning merely by finishing later. `BodyCompanionPublisher.swift:201` distinguishes readiness, training load, workout minutes, and metric-pull timestamps; `Store:6427` does not re-arm dashboard TTL on workout-only refresh. Preserve these instead of collapsing freshness to `generatedDate`.
- **Warnings:** source discovery for the headless engine exists (`Engine:2194`); high-HR warnings require usable workout exclusion intervals; delivery failure does not mark the ledger. The evaluator deadline already uses the fixed one-shot race. Fix F7, not the superseded RP-02 deadline bug.
- **Widget derivation:** widget payloads are built from cached app data, not independent widget HealthKit queries; sleep is aged with `asOf` at build (`HealthWidgetSnapshotBuilder.swift:52`). Changing display preferences can rebuild them without a HealthKit fetch. A rebuild timestamp is not evidence of new HealthKit data.

## 5. Structural changes that pay for themselves

No file-size target, extension shuffle, general persistence framework, or metrics-algorithm rewrite is proposed.

1. **One captured-context admission rule (P1):** replaces repeated incomplete cache/generation checks where their missing dimension causes F1. Keep thin orchestration methods and explicit leaf outcomes.
2. **One dashboard persistence envelope and acknowledgment (P2):** removes repeated opportunities to stamp metadata separately from payload. Preserve FIFO queue ordering and sidecar independence.
3. **One per-leaf outcome/coverage representation (P6a):** fixes all-or-nothing history repair and gives later dirty scheduling a precise completion signal. Keep the explicit field merge that protects live derived/intraday state.
4. **One canonical workout input owner only if P6b/P8 earn it:** existing month enrichment, record scanning, and training-load reads need different detail/coverage shapes. Do not combine their queries solely because all return `WorkoutSummary`. Share validated canonical membership/compute inputs, then retain independent detail enrichment and presentation.

The summary/history sleep overlap and summary/series training-load computations were examined; neither justifies a new shared fetch path in this pass. Existing leaf descriptors already centralize much ordinary quantity dispatch (`Engine:3249,3287,3325`); extending that into a generic refresh framework has no demonstrated benefit.

## 6. Measurement and verification gates

### Current measurement result

No comparable phone runs were captured. Initial sandbox device/simulator inventory failed with CoreDevice/CoreSimulator connection errors; a read-only unsandboxed `devicectl list devices --timeout 15` succeeded and showed both iPhones unavailable. No app was installed, relaunched, or its Health database modified for benchmarking. The available iPad is not substituted for the user's prior phone/database baseline.

`BodyPerformanceSignposts.swift:53,63,114` already records leaves, counts, effort candidates, and pool high-water marks and dumps at `finishRefresh`. Use it unchanged. Full-refresh `beginRefresh` supplies wall time; the warm workout-only path can report `total=n/a` and needs the existing `WorkoutMonths` interval plus externally measured entry-to-usable time. Leaf durations overlap, so their sum is not wall time; post-refresh background work can fall outside the foreground dump.

### Required baseline before performance-dependent P8 or budget/window tuning

Same physical phone, Health DB, build configuration, layout, permissions, primary/secondary groups, timezone, and settled background-backfill state. Three runs each:

| Path | Capture |
| --- | --- |
| Cold launch with dashboard >5 minutes stale, `.passiveResume` | Launch→first usable frame, `DashboardSnapshotLoad`, `DaySamplesSidecarLoad`, `RefreshRecentMonths`, per-leaf table, first summary/trend paint, phase-two completion |
| Home pull, `.userInitiated` | Same refresh intervals, three-month workout completion, effort candidates, write counts, early card publication |
| Warm resume 60 seconds–5 minutes | Entry→usable, `WorkoutMonths`, query/pool counts, verify vitals TTL does not advance |

Record each run and median/range, cache sizes, query count by type, and interactive/background overlap. Repeat after a behavior step only when it changes the measured path. For P8, compare query-derived and disk-derived inputs/results before timing; measure warm clean reuse, mutation repair, and bootstrap separately. No numeric speed target is proposed before these measurements. Correctness P1–P7 does not depend on an assumed speed win.

### Phase 2 gates, after explicit approval only

Each implementation commit contains one logical plan step, corresponding behavior tests, and synchronized `README.md`, `TestPlan.md`, and relevant documentation guards in `ProjectConfigurationTests`. Fetch tests use the real `BodyHealthQuerying` / `FakeHealthStore` seam, extended as necessary for anchored batches and controlled suspension; property-only or source-string tests are insufficient for races and persistence failures.

After **each** commit-sized change: build Body, run `BodyTests`, and run the repository's watch build/test gate whenever shared query types, calculator inputs, watermark/publisher behavior, or watch-consumed models change. A simulator launch/preflight failure must be reported separately from compile or assertion failure; a fallback build does not count as a passing test suite. Retain the 120-second deadline, bounded rings, input-only clamps, early publication, coherent abandonment snapshot, atomic writes, and history-preserving coverage replacement in every step.

No changes to `MARKETING_VERSION`, `CURRENT_PROJECT_VERSION`, `VersionHistory.md`, or version guards. No dashes in new user-facing copy. No unrelated cleanup. If an approved approach proves incorrect in implementation, stop and report the flaw rather than reinterpret this plan.

## 7. Suggested implementation sequence and approval scope

| Order | Logical step | Depends on | Scope of approval requested |
| --- | --- | --- | --- |
| 1 | P1 captured-context admission and scoped fallback | None | Correctness fix, including late publications and source failure |
| 2 | P2 atomic dashboard progress/freshness envelope | P1 for captured identity | Correctness/migration fix; retain old history |
| 3 | P7 warning reentrancy and pre-submit revalidation | P1 context semantics | Independent small correctness fix |
| 4 | P4 source discovery invalidation | P1 | Correctness/freshness fix, no new source cache |
| 5 | P5 aggregation-context and rollover invalidation | P1/P2 | Correctness fix preserving workout/sleep attribution policy |
| 6 | P3 explicit raw repair and bounded cache revalidation | P1/P2/P4/P5 | Split into intraday repair, effort validation, and historical/month repair commits; each separately tested |
| 7 | P6a per-leaf historical reconciliation | P1/P2/P5 | Correctness fix with bounded retention for rolling trends only |
| 8 | P6b workout journal and anchored invalidation | P1/P2/P3/P6a; capture §6 baseline first | Separate schema/transaction, fetch seam, and reconciliation commits; device proof required |
| 9 | P9 observer and background scheduling | P6b durable dirty protocol, P7 | Separate foreground dirty scheduling and background delivery commits; no notification coupling |
| 10 | P8 reuse persisted canonical training-load inputs | P6b; parity and measured benefit | Conditional follow-on, not an unconditional performance rewrite |

P6b's first rollout is deliberately limited to workouts. Quantity-type anchors, sleep deltas, and independent effort relationship change handling require their own verified dependency mapping before broadening it. Until then P3's explicit/periodic repair stays active. Frozen morning observation semantics remain unchanged. Approval of the plan does not authorize an implementation to guess at mutation coverage or skip any failed gate.

## 8. Adversarial review corrections

The separate read-only reviewer completed the review on 2026-09-04. It supported F1/F2/F3/F4/F6/F7 from source and agreed F5 should remain an inference pending a controlled timezone reproduction. The following corrections were checked against the code and folded in:

1. **Authoritative empty sidecar repair:** F3/P3 now distinguishes successfully queried empty coverage from unhydrated/default emptiness. The existing matching-scope preservation guard (`HealthDashboardSnapshotStore.swift:318`) would otherwise resurrect the final deleted samples after reload. The green hydration section is qualified accordingly.
2. **Compute-context admission:** P1 now includes sleep goal and effective compute/layout preferences, with separate fetch/compute projections to avoid needless raw refetch. The detached path captures these at `Store:5901,5939`; a fetch-only signature would leave the race open.
3. **Partial durability acknowledgment:** P2 now requires separate main-envelope/sidecar outcomes. A successful main write cannot acknowledge failed raw-sample persistence or clear its dirty coverage.
4. **Frozen observation protection:** P5 explicitly keeps aggregate timezone invalidation out of the frozen observation reset signatures. A global signature extension would clear history and violate the intended freeze contract.
5. **Reference corrections:** method anchors for source changes, ring checkpoints/persistence, sidecar signatures, snapshot save, and the notification ledger now point to the current definitions/statements.

No implementation, tests, versions, or app copy were changed during this review. Remaining evidence limits are explicit: no device reproduction, no new timing baseline, and no claim that every unmodified leaf/calculator has been exhaustively proven correct.

## 9. Implementation phases and acceptance checklist

These are implementation phases, distinct from the original two-part review/approval workflow. The user committed Phases 1–3 and Phase 4a, then requested **Implementation Phase 5** and deferred Phases 6 and 7. The subsequent instruction is to finish Phase 4's remaining work first; this includes the documented 4b correction and does not waive Phase 5's device-evidence gate. **Do not commit unless the user explicitly asks.** References below to commits describe logical change boundaries, not permission to create commits. The detailed findings and persistence contracts above remain binding.

### Implementation Phase 1: context admission and scoped fallback (P1)

**Build allocation (user authorized 2026-09-04):** all RP-03 phases stay on app version 1.1.0 build 8. App, widget, watch, and test targets plus their configuration/documentation guards move together; no further build increment is implied by completing a phase. This does not authorize committing or starting later phases.

**Work:** capture immutable query and compute identity before asynchronous work; invalidate in-flight admission synchronously when selections change; reject late progressive/final results and freshness stamps; reuse cached summary, daily series, and raw samples only within compatible source/permission scope. Include effective group membership, sleep parsing preferences, calendar identity, sleep goal, and effective compute/layout settings. Retain separate cache-reset and abandonment fences. Settings changes must leave a corrective refresh for the latest context, not silently lose it behind a busy refresh slot.

**Commit boundaries:** first implement the in-flight settings-context fence and detached-compute admission (P1a); then implement compatible cache fallback, effective resolved-source provenance, durable scope admission, and corrective scheduling (P1b). Verify each commit separately. P1a alone does not fix F1's no-concurrency source-failure scenario; Phase 1 is complete only after both land. No progress/freshness envelope migration, new query type, TTL tuning, or calculator formula changes.

**Acceptance:** scripted A→B with B failure cannot return A as B; suspended queries/compute cannot publish after source/group/permission changes; timeout recovery remains coherent; rename-only group edits preserve compatibility; unrelated compatible metrics remain usable. Rejected refresh bodies release their anchor without clearing a newer generation's anchor. Explicit settings actions retain user-initiated refresh scope through coalescing. Check persisted reload and widget/watch inputs as well as Home/readiness. Build Body, run all `BodyTests`, and run the watch gate because companion inputs are affected. Synchronize README, TestPlan, and applicable documentation guards with the logical change, without committing unless asked.

**P1b persistence contract:** provenance occupies the existing atomic dashboard summary-context field plus optional per-metric manifests in sidecar schema 3. Its invalidation key is effective requested source/group membership and discovered source identities per metric, permissions and entitlement resolution, sleep parsing flags, and calendar aggregation identity; readiness also depends on the sleep goal and calculator version. Overnight sleep vitals follow their individual source dependencies. Before discovery, a matching request can retain its previously proven membership for cold paint; discovery reconciles it before dashboard queries. This is not a new discovery cache or mutation detector: P4's session-latch limitation still applies. Legacy/unprovable daily and raw data fails closed once. Repair uses the existing successful dashboard/metric refresh and lazy intraday load, without reinstalling. Failure under a new scope leaves affected values absent, not relabeled old-source data; unrelated compatible fields remain usable. An incorrect scope could contaminate readiness or a companion seed, so context changes also retire relevant publication tokens and input watermarks. Frozen observations retain their existing source-reset policy; an aggregation-only change does not change their recorded context. Authoritative-empty writes within an unchanged scope remain P3, not part of this migration.

### Implementation Phase 2: durable progress and freshness (P2)

**Depends on:** Phase 1's captured identity.

**Work:** version the dashboard envelope to bind ring progress/completion, secondary scope, and durable freshness to its payload. Distinguish written, unchanged-valid, and failed saves. Migrate legacy checkpoints conservatively without discarding history. Keep main-envelope and sidecar acknowledgments independent, and keep progressive paint independent of disk latency.

**Acceptance:** injected write/encode failures and process-exit boundaries never let progress exceed durable coverage; reload retries uncommitted work; main success cannot acknowledge sidecar failure. Build/test/watch gates and synchronized docs accompany the envelope/migration commit.

**Delivered:** dashboard envelope version 1 now binds progress, comparison scope, and settled full-refresh freshness to the payload in one atomic file. All ten save paths capture metadata before enqueueing; ring chunks still paint immediately, and intermediate/abandonment saves do not adopt a newer live-only freshness date. Cold start trusts matching envelope context and fetch tiers, not independent defaults checkpoints. Main and sidecar outcomes are independent; preserving an unhydrated empty request is explicitly not an authoritative-empty acknowledgment. A failed write keeps the prior file, and the current in-memory candidate remains available for a later compatible save without a refetch. There is no new retry timer or cross-file transaction. Legacy/unknown metadata retains decodable history and restarts unproven progress. Deterministic boundary tests reload immediately before each atomic replacement; a physical-device process-kill run remains a manual check, not a claimed measurement. No performance claim or TTL change is part of this phase.

### Implementation Phase 3: consumer and context freshness (P7, P4, P5)

**Depends on:** Phase 1; P5 also needs Phase 2's durable identity.

**Work, separate commits in this order:** (1) revalidate warning eligibility immediately before submission and prevent actor-reentrancy ledger loss; (2) add force/dirty/expiry source discovery and propagate resolved-membership changes; (3) add aggregate timezone/day-rollover reconciliation while retaining raw instants and original frozen-observation attribution.

**Acceptance:** suspended warning evaluation respects changed settings/foreground state; changed source lists are detected with unchanged preferences; timezone travel and midnight invalidate only the appropriate aggregates. Each commit gets behavior tests, Body build, full BodyTests, applicable watch gate, and synchronized docs. No new source cache or frozen-history reset policy.

**P5 implementation and persistence contract:** Phase 1 already supplied calendar identifier, time-zone ID, and aggregation-policy version in the durable scope and per-metric hourly-sample manifests. Phase 3 adds an optional local `summaryDayStart` to that same atomic scope, captures it in refresh admission, and routes active-app calendar-day/time-zone notifications through the existing corrective owner. Its invalidation key is the current local start of day plus the existing aggregation/source identity. Legacy scopes with no day key revalidate today's fields once, retaining historical series. Repair is the existing full dashboard or explicit metric refresh; failed queries cannot restore incompatible old-zone series. The cost of a wrong day key is yesterday's totals, warnings, or current derived state presented as today, so input freshness and companion seed watermarks are retired too. Midnight does not expire historical trends, raw samples, latest instantaneous readings, or wake-attributed sleep. Zone changes use the already scoped daily/hourly invalidation without changing frozen-record reset signatures or the workout time-zone ledger. Warning admission now captures a live calendar/day rather than keeping the launch-time calendar. This is provenance and scheduling work, not a measured performance claim.

**P5 plan gap, subsequently reviewed and approved:** before this amendment, `HealthKitFetchEngine+ActivityRings.swift:550–556` converted HealthKit date components to a midnight `Date`; `BodyMetricsKit/ActivityRings.swift:79–86` persisted only that instant and the summary, and `calendarMonths` at `:190` regrouped it using the current calendar (pre-amendment line references). September 4 New York midnight could therefore display as September 3 in Los Angeles. The aggregate scope did not repair ring history or its completed checkpoint. Implementation paused and the user explicitly approved a historical day-identity migration before work continued. The new travel and month-boundary regression tests reproduce the relevant conversion and replacement paths; physical-device observation remains a manual check.

**Approved amendment, implemented:** ring records preserve explicit Gregorian year/month/day labels alongside their original instants. Display and replacement use those labels, and partial-backfill metadata carries its exclusive resume day so travel cannot shift a checkpoint. Existing typed day labels are preserved on reload. Pre-amendment records have no per-entry zone proof: the latest dashboard aggregation zone cannot establish the origin of every historical ring entry. Such records remain in an unattributed archive, excluded from dated counts until authoritative repair, rather than guessed into a new day. **Invalidation key:** `ringDayIdentityVersion = 1`, explicit day labels, and successful month coverage carried in the atomic envelope; old unproven checkpoints restart once, while new partial repairs resume. **Repair:** the existing bounded backfill records successful coverage even for empty months. An unknown record is retired only after all possibly corresponding months have validated (a conservative two-day UTC margin handles boundary ambiguity). Remaining archive months are prioritized by older-month pagination, at most three queries per pass, including dates outside the automatic ten-year window. Failure supplies no coverage; an unsuccessful disk write retains the prior archive/checkpoint for retry. **Cost of being wrong:** misdated closed rings and historical counts; no readiness-notification dependency. On upgrade, old counts can temporarily be unavailable while their archived values remain on disk and repair proceeds. Verified first-of-month records bypass the old truncation heuristic. Frozen readiness/Radar observation attribution and the workout ledger are unchanged. No query deadline/budget or atomic-write rule changed.

**Progress:** P7 adds a captured settings identity, final authorization/foreground/context/cancellation admission, one in-flight evaluation owner, and post-add ledger merging. Suspended-submission tests cover stale settings, concurrent seeding, overlap, and failed delivery retry. Its initial Body build passed and full BodyTests passed 2,243 with 1 skip (`/private/tmp/body-rp03-p3-warning-full.xcresult`). Headless thresholds are also frozen at engine creation so settings cannot change query inputs during discovery; foreground engines retain live thresholds. P4 adds force/dirty/per-kind 24-hour discovery expiry and an in-flight discovery generation fence, preserving failed maps and publishing known-empty watch expectations. Existing source-map revision admission remains authoritative. A generic shared grouping overload enables nonempty source-list regression fixtures without fabricating HKSource instances; fake-store tests cover the actual discovery control path. P4 passed its build/full-suite/watch gates. P5 and its subsequently approved ring-history amendment passed the final full suite and build/watch gates recorded below. Observer delivery itself remains in the later observer phase; known in-app writes already dirty discovery. The user committed Phase 2 and explicitly retained its conservative legacy migration; no legacy completion flags will be imported without durable coverage proof. No commits are authorized for this phase.

### Implementation Phase 4: explicit repair and historical coverage (P3, P6a)

**Depends on:** Phases 1–3.

**Work, separate commits:** (1) explicit retained-window intraday reconciliation and authoritative-empty sidecar writes; (2) effort validation timestamps and bounded revalidation; (3) loaded-month validation and resumable historical/record repair; (4) per-leaf historical reconciliation and input revisions so one failed leaf does not discard successful repairs. Retain the passive overlap and existing query-budget/deadline limits.

**Acceptance:** add/delete/late-write cases across the overlap, old months, and prior years repair through the documented user gesture or bounded policy. Successful empty clears only authoritative coverage, failures preserve it without marking it validated, and relaunch does not resurrect deletions. Run all commit gates and synchronize docs after each logical change.

**Phase 4a delivered and committed (2026-09-04):** explicit sample-based metric pulls reconcile their complete retained intraday window; passive reads retain the 48-hour overlap, and hourly cumulative reads retain their existing full-window queries. Per-series success authority distinguishes an empty repair from a failed or unqueried field. All ten dashboard writers capture that authority with the payload and scope. Compatible unqueried sidecar fields survive partial saves. A failed atomic write retains the prior file; the admitted live result remains available for retry. Session authority also masks obsolete memoized hydration, and per-series raw-data revisions reject older colliding lazy results without discarding unrelated fields. No extra cache schema, TTL, timer, or performance claim is introduced. The raw quantity leaf now uses the existing cancellable `BodyHealthQuerying.samples` seam with the same request, pool permit, and query telemetry, enabling real scripted reads rather than callback-count tests.

**Phase 4a persistence contract:** the invalidation key remains each primary/comparison series' permission and effective-source scope, with aggregation identity for hourly buckets. Successful query coverage supplies write authority; context admission and per-series raw-data revisions prevent obsolete results from claiming it. Repair is the explicit metric pull, with a later compatible save retrying a failed write; relaunch reloads the last durable state and can repeat the pull. Wrong raw values can affect charts and stress inputs, so a failed query cannot acquire authority or validate an empty result. This is explicit reconciliation, not detection of arbitrary HealthKit changes between pulls.

**Phase 4a gates:** generic iOS Body build passed. Full BodyTests passed **2,279 with 1 skip, no failures** (`/private/tmp/body-rp03-p4a-full-2.xcresult`); watch tests passed **85 with 1 skip, no failures** (`/private/tmp/body-rp03-p4a-watch.xcresult`). Six new `IntradayRepairTests` cover retained-window deletion/late writes, failure, cancellation, partial durability/retry/reload, and admitted versus rejected publication with obsolete hydration. The initial full run exposed a source guard still expecting `resolvedTrend`; it now requires `resolvedDaySamples` for all three comparison-source raw paths. Physical-device mutation/relaunch checks remain manual. Versions and schemes are unchanged.

**Phase 4b dependency gap, subsequently approved (pre-4b line references):** TTL selection alone cannot meet the plan's downstream freshness contract. `HealthKitFetchEngine.swift:2973–2977` removes failed effort IDs when a cached score is available. `fetchWorkoutSummaries` (`:2480–2509`) reduces effort outcomes to display values and an unresolved flag that is false for cached fallback (`:2807–2813`). The shared Training Load fetch returns only `[WorkoutSummary]`; its summary/series/seed paths treat that return as success (`HealthKitFetchEngine+TrainingLoad.swift:81–144`). A successful seed is stamped with the current refresh date (`HealthKitWorkoutStore.swift:7008–7013`). Thus an expired effort revalidation failure could retain the old score correctly yet incorrectly renew derived freshness. This is verified control-flow evidence; no device reproduction is claimed.

**Sequencing correction, approved and implemented in 4b:** effort-validation outcome propagation lands before 4b's acceptance gate rather than waiting for 4d. Cached workout values remain available, but the Training Load shared-input path reports failed validation so its summary, series, and seed cannot renew freshness from fallback scores. `effortUnresolved` retains its display meaning. The existing cancellable `BodyHealthQuerying.samples` seam now also carries the workout-list read, preserving its predicate, ordering, query budget, and telemetry.

**Phase 4b delivered, uncommitted (2026-09-04):** found scores and confirmed absences carry an optional successful validation timestamp. The invalidation key is workout UUID plus start/end dates and a 24-hour validation interval; legacy missing timestamps, date changes, expiry, and clock rollback are stale. Successful empty reads remove a previously cached score. Failure retains values and the old validation date, and cannot refresh the Training Load summary, series, or watch seed. Per-workout result revisions and the existing clear generation reject superseded suspended queries. Repair remains a Training Load full pull, a scoped workout-month pull, or Clear Cache; a failed durable write leaves the older ledger for revalidation after relaunch. The cost of being wrong is stale Training Load/readiness input, so display fallback is not proof of fresh input. No notification or automatic-write authorization is inferred from this cache.

**Phase 4b verification:** five new `WorkoutEffortValidationTests` exercise expiry, cached failure, deletion, late rating, legacy decode, clock rollback, changed dates, disk reload, clear during a suspended query, and all three Training Load consumers failing closed. Generic iOS Body build passed. Full BodyTests passed **2,287 with 1 skip, no failures** (`/private/tmp/body-rp03-p4b-clean-full.xcresult`); watch passed **85 with 1 skip, no failures** (`/private/tmp/body-rp03-p4b-watch.xcresult`). An incremental-directory run repeatedly reported the old source-guard failure after its assertion was updated; the isolated guard and full suite both pass from a fresh derived-data directory. No assertion was removed or weakened. Physical-device mutation checks remain manual. README, TestPlan, and documentation guards are synchronized; versions and schemes are unchanged. This was the 4b gate; subsequent 4c/4d delivery is recorded below.

**Phase 4a review corrections (2026-09-04):** sample stripping and write-authority rejection now share the same admission condition. Replaced the global raw revision with per-series revisions and partial publication: an overlapping Steps result cannot discard successful oxygen/HRV fields, while repeated same-series repairs still fence older results. Sidecar preservation now decodes only when empty non-authoritative fields exist, reuses that decode for the all-empty guard, and reuses its bytes for dedupe. Regression tests count one read and at most one decode; no elapsed-time improvement is claimed. Updated README, TestPlan, and their guards. Body build passed; full BodyTests passed **2,281 with 1 skip** (`/private/tmp/body-rp03-p4a-review-full.xcresult`); watch passed **85 with 1 skip** (`/private/tmp/body-rp03-p4a-review-watch.xcresult`). No failures. This was the pre-4b gate; the subsequently approved 4b work and its newer gates are recorded above.

**Phase 4c dependency gap, subsequently approved (pre-4c line references):** successful workout membership is not proof of successful record-input validation. `HealthKitFetchEngine.swift:3195–3235` preserves failed associated-distance IDs internally, but `fetchWorkoutSummaries` (`:2477,2522–2526,2560`) converts those failures to cached/nil display values and returns only summaries. `HealthKitWorkoutStore+Records.swift:175–180` supplies no cached summaries; `:192–210` then upserts the returned contributions and advances `scannedThrough`. `WorkoutRecordLedger.swift:105–115,198–203` replaces a contribution with whatever scalar values the summary contains. Concrete scenario: a previously known running workout has no `HKWorkout.totalDistance`, its associated-distance statistics query fails during a revisit, and its returned summary lacks distance; reusing the current backfill assembly for rolling repair would remove that workout's distance/pace contribution while recording successful coverage. The existing baseline can already encounter the same condition after an earlier month fold. Confidence was high from control flow at the pause; the approved implementation now includes the scripted reproduction described below.

**Phase 4c correction, approved and implemented:** expose record-input validation alongside display summaries before adding month-validation timestamps or historical checkpoints. Keep progressive month publication, reconcile membership only after a successful workout-list read, retain existing contributions for failed record inputs, and do not validate/advance a historical chunk until all required record inputs succeeded. Unrelated HR/VO₂/effort failures must not invalidate a successful distance repair or suppress early workout display. Successful empty distance reads must remain distinguishable from failures. Apply the same rule to the existing baseline and the new rolling repair so they cannot disagree about coverage. This is a fetch/record admission correction within 4c, not a broad fetch-engine restructuring. Add fake-store failure/empty/retry tests with a pre-existing record contribution, plus persisted checkpoint reload assertions, before the usual app/full-suite/watch gates. The user approved this correction before implementation. The remaining work follows the original 4c/4d order.

**Phase 4c implementation record (2026-09-04, uncommitted):** successful membership and record-input validation are separate fetch outcomes. Progressive months publish display fallbacks but retain failed/unqueried record contributions; baseline chunks cannot advance through failed distance reads. A value projection of the existing cancellable statistics seam makes success, empty, failure, and cancellation scriptable without constructing `HKStatistics`. Visible Workouts chart/selected months revalidate after five minutes on active scene entry; navigation also revalidates stale cached months while displaying them immediately. Optional validation metadata is distinct from `generatedAt`, with unchanged-content metadata saves retaining that content timestamp. Month publication checks context, engine revision, cache epoch, deadline, and per-month request revision; weekly coverage requires actually admitted months.

**4c persistence contracts:** month validity is keyed by permission selection, calendar identifier/time-zone ID, policy version, and successful validation time; absent metadata and rollback are stale. Cold launch still treats month files as display seeds, not session query coverage. Explicit month pull repairs membership/details and record contributions. Wrong values affect displays and derived inputs, so incomplete detail reads cannot renew month validation. Record/ring rolling cursors live in the record ledger/dashboard envelope respectively, never a separate acknowledgement. Their keys include permission/calendar/policy context and the last successful covered month; they reset on context change/rollback or Clear Cache. Each eligible pass revisits one previous month, at least five minutes apart; after reaching the known floor, a cycle waits 24 hours before restarting. Records probe HealthKit's earliest workout and retain the older cached floor so deleting the oldest workout remains repairable. Rings revisit the ten-year window plus older retained months. These intervals are conservative bounded-work policies, not measured performance improvements or guaranteed wall-clock freshness. Successful empty coverage removes only that interval; failures, superseding publication, and rejected context do not advance the cursor. A failed atomic write reloads the prior payload and cursor together. Explicit month pull/ring pagination and Clear Cache remain repair paths. Stale PR/ring displays have lower consequences than score freshness; no cursor authorizes a notification or a new watch watermark.

**4c verification so far:** record-correction Body build/full suite/watch passed (2,290 app tests and 85 watch tests, each with 1 skip). The full historical implementation then passed Body build, **2,296 app tests with 1 skip** (`/private/tmp/body-rp03-p4c-full.xcresult`) and **85 watch tests with 1 skip** (`/private/tmp/body-rp03-p4c-watch.xcresult`), no failures. Added failure/empty/retry/cancel, persisted checkpoint, bounded interval deletion, expiry/context/rollback, metadata-only dedupe, cycle/relaunch, and injected ring-write failure tests. The final post-await admission tightening also passed the full suite, 2,296 tests with 1 skip (`/private/tmp/body-rp03-p4c-final-full.xcresult`). Successful passive reuse preserves, but never renews, the previous compatible validation timestamp; actual query failure retires it. Visible-month freshness is evaluated after waiting for the refresh slot, avoiding redundant revalidation of a just-completed refresh. Physical-device mutation/resume checks remain manual; no stopwatch claim is made. README, TestPlan, and documentation guards are synchronized.

**Phase 4d implementation record (2026-09-04, uncommitted):** full-window repair publishes independently successful primary/comparison leaves instead of rejecting every correction when one leaf fails. Successful empty coverage removes that rolling series; failure retains compatible live data and cannot certify full-refresh freshness. Sleep coverage also requires successful nocturnal-vital hydration. Each leaf captures its input revision, so a newer same-series publication rejects only the colliding old result. Raw intraday samples, frozen observations, Stress, and the independent Training Load system are outside this replacement map. A separate live-data revision guards the suspended derived recompute; if it becomes obsolete, persistence saves the current live snapshot, not that old compute result.

**4d persistence contract and costs:** this reuses the existing atomic dashboard envelope rather than adding an artifact. Invalidation remains captured source/permission/aggregation/selection context plus cache epoch and per-leaf publication revision. Successful full-window queries repair retroactive edits/deletions; explicit metric refresh and Clear Cache remain user repair paths. Rolling leaves are bounded to the existing trend retention start, including failed cached tails; frozen history and raw samples are not pruned. Incorrect history can feed current derived inputs, so a partial repair cannot renew a complete freshness claim or stamp a new watch watermark. Costs are bounded series comparisons, a daily-statistics value projection for scripted tests, and the existing full-window background queries. Query options, input-tier clamps, budgets, deadline, and progressive publication remain unchanged. No measured performance improvement is claimed.

**4d verification:** four new `FullTrendReconciliationTests` cover real fake-store partial success, successful empty coverage, revision collisions, retention and frozen-history preservation, failed sleep-vital coverage, and rejection of a suspended stale derived commit. Focused reconciliation/failure semantics passed 22 tests (`/private/tmp/body-rp03-p4d-focused-2.xcresult`); Body build passed and watch tests passed 85 with 1 skip (`/private/tmp/body-rp03-p4d-watch.xcresult`). The initial full suite passed 2,300 tests with 1 skip and failed two fixed-width source guards: new leaf arguments moved protected fields outside their 5,500-character slice. Both guards now delimit the complete initializer and retain every field assertion. Final full-suite rerun passed **2,302 tests with 1 skip and no failures** (`/private/tmp/body-rp03-p4d-full-final.xcresult`). `git diff --check` is clean; build 8, version guards, and schemes are unchanged. Physical-device mutation/resume and §6 timing evidence remain outstanding; simulator tests do not substitute for those checks. The first focused run also exposed a test fixture with Pro comparison disabled; enabling and restoring the entitlement made the intended comparison failure exercise the real fetch path. No production behavior or assertion was weakened to accommodate these failures.

**Phase 4 follow-up review (2026-09-05, uncommitted):** all three reported regressions were verified. Concurrent expired effort gathers could reject each other's successful results after the first advanced the per-workout revision. A superseded successful gather now reuses the current compatible, fresh validation without overwriting it, including authoritative absence; changed workout dates, clear/cancellation, and real query failures still fail closed. No-op record folds now skip publication/persistence, and the publication revision changes only when persisted ledger content or progress changes, so empty visible-month revalidation cannot invalidate an unrelated historical chunk. The Workouts navigation branch now includes already-loaded empty months in immediate display plus settled background revalidation; unknown, cleared, and empty disk-only months still wait. Added overlap and ledger-revision behavior tests plus the UI branch guard; README/TestPlan/documentation guards are synchronized. The whole-year baseline rejection on an unvalidated record input remains intentional and unchanged: it cannot certify complete record coverage through an unresolved distance. This can delay records until the failing input is successfully repaired. Verification passed: focused regressions 13 tests; generic iOS Body build; full BodyTests **2,305 passed with 1 skip and no failures** (`/private/tmp/body-rp03-p4-review-full.xcresult`); watch **85 passed with 1 skip and no failures** (`/private/tmp/body-rp03-p4-review-watch.xcresult`). No device or performance claim is added. Build 8, version files, and schemes remain unchanged; no commit was created.

### Implementation Phase 5: anchored workout journal (P6b, separate approval gate)

**Post-activation verification (2026-09-06):** focused lifecycle/documentation tests passed **12 / 0 skips / 0 failures** (`/private/tmp/body-rp03-p5-activation-focused.xcresult`); full BodyTests passed **2,332 / 3 skips / 0 failures** (`...-activation-full.xcresult`); watch passed **85 / 1 skip / 0 failures** (`...-activation-watch.xcresult`). The ordinary signed iPad Body build passed with no validation compilation flag and was installed preserving app data. Direct launch succeeded as PID 5341; the user completed a Home pull and confirmed completion. `/private/tmp/body-rp03-activation-attached-20260906.trace` records execution and stopping of an `HKAnchoredObjectQuery` for workouts, one completed 10.890-second refresh (44 leaves, peak 11 queries, interactive 10/background 1, 272 effort candidates), and no logged HealthKit trend-query failures. These are smoke-test observations, not matched performance comparisons. The initial Instruments path-based launch failed in its application-info lookup; it is not counted as a successful launch or timing run. The attached capture finalized automatically.

The post-refresh journal is identical to its pre-activation checkpoint: revision 47, same scope/generation/anchor, 458 members, no dirty intervals and no full repair. The saved dashboard has completed freshness dated 17:00:51 UTC and retains 2,395 ring days; the record ledger remains complete with 2,340 contributions (`/private/tmp/body-rp03-activation-refreshed-{journal,dashboard}.json`, `...-running-records.json`). Thus the ordinary enabled lifecycle queries the existing anchor without restarting bootstrap/repair on this unchanged database. No HealthKit mutations or cache resets were performed. The enabled build remains on the iPad. Phase 5 implementation and activation checks are complete; the explicitly incomplete matched performance baseline remains separate measurement work, not a claimed speedup. `git diff --check` is clean, versions/schemes are unchanged, and nothing was committed.

**Production activation approved (2026-09-06):** after the completed iPad repair below, the user confirmed correct display and refresh behavior, reported code review complete with no must-fix findings, and explicitly approved enabling and testing the journal. `WorkoutChangeJournalStore.lifecycleEnabled` is now true for ordinary Debug and Release construction. The temporary `BodyApp` validation-only override was removed; authorization admission, foreground scheduling, retry backoff, query-derived freshness and durable acknowledgment remain unchanged. README, TestPlan and activation guards now describe the enabled contract, including explicit test opt-out. This supersedes historical disabled/pending-approval statements below. Post-activation automated gates passed: 2,332 app tests with 2 device-only skips and 86 watch tests, no failures. On-device post-activation results are pending. The matched performance baseline remains incomplete and no speedup is claimed. Phases 6 and 7 remain deferred and build stays 8.

**Completed dependent repair on iPadPro (2026-09-06):** after the scoped no-data correction and gates below, the local validation build repaired all 15 journal months from July 2025 through September 2026 in three-month chunks. Four controlled process relaunches retained completed checkpoints and advanced later chunks. Before the final dashboard/save gate, all 458 dirty intervals remained pending; afterward the persisted journal reached revision 47 with 458 members, zero dirty intervals, `requiresFullRepair == false`, and no remaining repair progress (`/private/tmp/body-rp03-nodata-final-journal.json`). The final dashboard envelope contains completed freshness dated 16:24:38 UTC, 2,395 ring days and zero unattributed ring days; the record ledger has `baselineComplete == true` and 2,340 contributions (`...-final-dashboard.json`, `...-final-records.json`). This supersedes the September 5 unresolved no-data repair blocker below. No HealthKit records were created, edited or deleted during this run.

The automatically finalized final repair capture (`/private/tmp/body-rp03-nodata-final-repair-20260906.trace`) reports a 6.880-second refresh with 44 leaves, peak four HealthKit queries and zero effort candidates. This is a single repair-tail observation, **not** the RP-02 §6 matched baseline protocol or a performance improvement claim. The ordinary signed build, without the device-validation compilation flag, was reinstalled and launched at 12:29 EDT, preserving app data. Production journal activation remains disabled. User visual confirmation of Home and workout records is still pending; this result does not complete every production activation/performance acceptance gate. Phases 6 and 7 remain deferred. No commit was created.

**Approved no-data correction (2026-09-06):** the user approved the scoped correction proposed below. Before implementation, the read-only native probe `WorkoutJournalDeviceValidationTests/testNativeWorkoutCumulativeNoDataErrorCode` passed on iPadPro (1 passed, 0 skipped/failed; `/private/tmp/body-rp03-native-nodata-20260906.xcresult`). It queried the previously deleted validation workout's associations and asserted `HKErrorDomain` plus `HKError.errorNoData` for step count, walking/running distance and cycling distance. It neither created records nor requested permissions. This verifies the runtime error identity previously inferred from localized logs. The temporary probe opt-in was removed afterward.

Steps and distance now share an app-private workout-association cumulative helper using `BodyHealthQuerying`, the existing task-local query budget, cancellation checks and query telemetry. It normalizes only a bridged `HKError` with `.errorNoData` to `.success(nil)`; no numeric zero is fabricated. Nil-error failures, authorization/locked-store errors, cancellation and unrelated domains remain failures. General statistics, shared/watch query semantics and permission filtering are unchanged. Routing the former direct cadence callback through the existing seam makes both paths testable without duplicate error handling. `WorkoutCumulativeNoDataTests` covers cached-value removal, month and record validation, real failures, unit preservation and cancellation; final gate/device results follow separately. This supersedes the unimplemented/approval-pending status of the no-data proposal below, not the production activation gate.

No-data regression gates: **17 focused tests passed** (`/private/tmp/body-rp03-p5-nodata-focused-2.xcresult`), followed by **two consecutive full-suite passes, each 2,331 passed / 3 skipped / 0 failures** (`...-nodata-full-2.xcresult` and `...-nodata-full-3.xcresult`). Watch passed **85 / 1 skip / 0 failures** (`/private/tmp/body-rp03-p5-nodata-watch.xcresult`). An initial outdoor fixture stalled in the unrelated native VO2 callback; making the synthetic run indoor kept testing scoped to cadence/distance. A subsequent initial full run stalled in a new test despite the isolated pass; a stack sample showed an async wait. New-test fetches now use a five-second cancellation deadline with query diagnostics; both full reruns completed without hitting it. The original full-run stall's exact cause was not proven, and the interrupted results are not counted as passes (`...-nodata-full.xcresult`, `/private/tmp/body-rp03-nodata-test-stall.sample`). No production workaround was added for that test issue.

**Approved pre-activation retry safeguard (2026-09-05):** the user approved continuing with bounded retries and physical lifecycle validation. Repair now checkpoints a per-month attempt before fetching, retaining its dirty interval on query failure, incomplete detail validation, persistence failure, cancellation, or process interruption. Foreground opportunities retry after 5 minutes, 30 minutes, 2 hours, then 6 hours, with the stored attempt counter capped at four; this is capped backoff, not abandonment after four tries. Eligible later months proceed even when an earlier month fails. Only validated, durably saved month/record results clear the attempt and complete that month. Optional attempt metadata preserves decoding of existing schema-1 checkpoints; source/permission/calendar context changes and Clear Cache reset progress. Clock rollback permits retry instead of stranding a future timestamp. The journal still cannot grant derived freshness or acknowledgment while any obligation remains. Cost: one small atomic journal checkpoint per attempted month; risk: transient failures may wait for the next eligible foreground opportunity, while ordinary query-derived refresh and explicit cache repair remain available. Focused verification passed 18 tests (`/private/tmp/body-rp03-p5-retry-focused.xcresult`), including repeated failure, cross-relaunch cooldown, later-month fairness, legacy decoding, and delay bounds. This supersedes the previously open failed-month retry note below.

The app-owned lifecycle can be injected only in a local `DEBUG && BODY_WORKOUT_JOURNAL_DEVICE_VALIDATION` build; the production gate remains false, with no saved preference or Release activation. Device lifecycle and final regression results will be recorded separately, not inferred from the compile switch.

**Retry regression gates and real relaunch validation (2026-09-05):** full BodyTests passed **2,327 with 2 skips, 0 failures** (`/private/tmp/body-rp03-p5-retry-full.xcresult`); watch passed **85 with 1 skip, 0 failures** (`/private/tmp/body-rp03-p5-retry-watch.xcresult`). Signed validation Body build passed using `OTHER_SWIFT_FLAGS='$(inherited) -D BODY_WORKOUT_JOURNAL_DEVICE_VALIDATION'`. An earlier command overriding `SWIFT_ACTIVE_COMPILATION_CONDITIONS` removed a Swift package condition and caused RevenueCatUI's duplicate `Bundle.module`; correcting the command fixed the build without dependency edits. Installed the validation variant at 21:52 EDT and launched it at 21:53. It bootstrapped 458 entries, retained 458 dirty intervals, invalidated dependent derived artifacts, and persisted month attempt checkpoints. The record baseline began rebuilding. The automatically finalized 90-second `/private/tmp/body-rp03-lifecycle-initial-20260905.trace` saved successfully; it attached after the initial foreground refresh and is **not** a launch or three-refresh performance baseline.

At 21:55 the app process (PID 4934) was force-terminated with repair obligations pending, then relaunched as PID 4940. Comparing `/private/tmp/body-rp03-lifecycle-prekill-journal.json` and `...-relaunched-journal.json` verified identical scope, generation, anchor, membership, and dirty intervals. Earlier cooldowns survived; later months were attempted rather than repeating the first three. This is real process-restart evidence for persisted pending work, **not** interruption at every atomic write boundary or proof of completed dependent repair.

**New activation blocker, implementation paused for approval:** no attempted month reached completed validation. The captured app error messages include **778 step-count, 85 walking/running-distance, and 11 cycling-distance** responses stating “No data available for the specified predicate.” Source evidence: `HealthKitFetchEngine.swift:3233` (`stepCadence`) maps a nil statistics result to failure; `:3321` (`workoutDistanceMeters`) maps the cumulative query's failure to a failed distance; `BodyHealthQuerying.swift:295` maps nil native statistics to failure. These failures feed `hasUnvalidatedDetails`/`hadQueryFailure` at `HealthKitFetchEngine.swift:2602`, and `WorkoutMonthSnapshot.swift:120` refuses validation. Distance failures also prevent record-baseline advancement. **Confidence:** the failure classification and its validation consequences are verified in code; runtime logs record localized descriptions, not error domains/codes, so identifying every response specifically as `HKError.errorNoData` remains to be verified in a focused native check. The local SDK's `HKDefines.h` describes that exact error as no data for the requested query/predicate.

Proposed narrowly scoped correction, **not implemented or approved yet**: first verify the typed native no-data outcome, then treat only `HKError.errorNoData` as successful absence in workout cumulative steps/distance reads. Keep nil-error malformed callbacks, authorization errors, locked-store failures, cancellation, and other failures non-authoritative. Do not change statistics semantics globally or manufacture zero-valued health metrics. Add native-seam/fake-fetch tests covering successful absence versus genuine failure, month validation and record input, then rerun full app/watch gates and physical dependent repair. Cost: small targeted fetch/seam changes and regression coverage; risk: overbroad error normalization could erase cached values or falsely validate denied data, so exact typed matching and unchanged permission filtering are mandatory. This is a plan correction requiring user direction under the house rule; bounded retry alone cannot make a semantically misclassified absence resolve.

Validation was stopped at 21:59 and the previously verified ordinary build 8 (without the validation compile switch) was restored, preserving data and evidence. The stopped journal still held all 458 entries/dirty intervals, 12 attempted months and no completed months. The derived record ledger had 2,171 contributions but `baselineComplete == false`, so records may remain unavailable until repair can finish; the pre-validation ledger/dashboard copies remain in `/private/tmp/body-rp03-lifecycle-before-{records,dashboard}.json`. No stale backup was silently written over newer derived state. No HealthKit records were deliberately mutated in this lifecycle run. Production activation remains off, Phases 6/7 deferred, and no commits were made.

**Device validation session, evening of 2026-09-05:** iPadPro was connected by cable, unlocked, and running iPadOS 26.6.1 with Body 1.1.0 build 8 installed. The existing installation was retained; build number alone does not establish binary provenance. The automatically finalized `/private/tmp/body-rp03-ipad-validation-home-20260905-2.trace`, attached to the exact app PID, contains two complete refresh profiles: **11.865 s and 10.862 s**, each with 44 leaves, 272 effort candidates, peak HK queries 11, and pools interactive 10/background 1. The per-leaf log export succeeded; Instruments crashed exporting both interval and raw signpost tables. These two profiles do not establish a new three-run baseline, launch-to-usable time, or journal correctness. The effort candidate population differs from the earlier 249-candidate captures, so no direct speed comparison is claimed.

The user then performed three additional refreshes during `/private/tmp/body-rp03-ipad-validation-home-third-20260905.trace`. The agent's manual stop terminated the recorder before finalization; export reports `Document Missing Template Error`, and the directory contains only `Trace1.run/RunIssues.storedata`, not recoverable measurement data. Those runs are **not counted as captured**, and no recording process remains active. An earlier name-based attach selected the widget extension and was discarded before the requested pulls; subsequent recordings used the app's exact PID. Use automatic time-limit finalization for subsequent captures, not Ctrl-C through the command wrapper. No new build was installed, no Health records were deliberately added/edited/deleted, production journal activation remains off, and no commit was created. Device mutation/relaunch validation remains pending; further baseline repetitions are paused rather than repeatedly burdening the user after tooling failures.

**Depends on:** Phase 4 and the §6 baseline protocol.

**Controlled physical mutation validation, 2026-09-05 at 21:39 EDT:** after explicit user approval, installed the signed current build 8 test host on iPadPro and ran only `WorkoutJournalDeviceValidationTests/testApprovedHistoricalAdditionAndDeletion`. **1 passed, 0 skipped, 0 failed**, test duration 4.564 s (`/private/tmp/body-rp03-device-mutation-20260905.xcresult`). The isolated native journal detected one marked, one-minute workout dated 30 days earlier; a newly constructed reconciler restored the identical journal and real HealthKit anchor from disk; exact-object deletion succeeded and the next anchored scan removed the UUID while retaining its dirty interval. Baseline membership remained present. The device's completion receipt was retrieved independently to `/private/tmp/body-rp03-device-mutation-receipt-20260905.txt`. The only workout created and deleted was validation UUID `3832E9F0-2464-4CAC-9958-FB79A92D722D`; existing HealthKit records were not modified. Its archived identity remains in the app-private, backup-excluded `RP03DeviceValidation` directory to prevent accidental repeat creation. The test requires an explicit environment opt-in and skips simulators and ordinary runs. The project skill kept this harness isolated from production persistence and avoided running cache-mutating tests on the user's device.

This closes the controlled native workout addition/deletion and journal disk-restoration check only. It does **not** establish actual process-kill/relaunch recovery, dependent month/dashboard repair, activated-build performance, or the remaining baseline runs. Production `lifecycleEnabled` remains false. The previously noted persistent failed-month retry/backoff issue remains a pre-activation follow-up; exhausting retries must not retire dirty obligations. Phases 6 and 7 remain deferred. No commit was created.

Harness safety verification: removed the temporary opt-in from the generated `/private/tmp` test configuration and reran the exact physical test: **1 skipped, 0 failures**, before any HealthKit authorization/read/write (`/private/tmp/body-rp03-device-optout-20260905.xcresult`). The simulator documentation guard passed (`/private/tmp/body-rp03-device-harness-guards.xcresult`); that incremental simulator run did not discover the new device test, so no simulator skip result is claimed. Signed physical build-for-testing passed. Existing full app/watch gates above predate this test-only addition; they were not rerun on the user's device.

**Phase 5 infrastructure delivery (2026-09-05, uncommitted):** the atomic schema-1 journal retains UUID/start/end/type/duration/source metadata, a securely archived HealthKit anchor, staged bootstrap membership, and pending repair intervals. It does not persist a second derived score or replace enriched month/detail snapshots. The query seam uses a one-shot `HKAnchoredObjectQuery` with the fixed lower-bound predicate and existing cancellable resume box; no observer is installed. The actor drains four 500-object pages by default, with a total maximum 120-second race that never joins a stuck loser. Pages commit individually; failure leaves the old anchor and obligations. One owner rejects overlapping scans and guards post-query commits against clear/restart, engine-context changes, and newer journal revisions. A terminal empty page ends staging but is not read-authorization proof or derived freshness. The subsequent user instruction to finish implementation also covers lifecycle/dependent repair; that wiring is now present behind a disabled production gate, as detailed below.

**Phase 5 persistence contract:** anchor identity is schema, installation UUID, fixed lower-bound predicate/version, and bootstrap generation. Lazy construction restores the scope from the excluded-from-backup, app-private journal envelope; a missing/unusable envelope starts a new installation identity with an initial 408-day lower bound. The predicate never moves between pages/relaunch. Clear removes the exact journal file, allowing a new scope on the next construction. Raw intervals remain absolute instants; repair applies current permission/source/calendar/calculator context. Moved workouts dirty the union of old/new intervals. Unknown deletions require full repair rather than guessing a month. Anchor decode/rejection resets cursor/staging while retaining the previous canonical generation; ordinary query failure does not reset it. Clear and workout opt-out fence construction/pages and delete the journal even before its owner is constructed. Encode/write failure never advances the disk anchor, and failed repair acknowledgment retains dirty work. Acknowledgment requires matching generation/revision, completed bootstrap, a still-valid publication token, and durable dependent repairs. Wrong membership could contaminate records/readiness/watch inputs, so journal membership supplies no scores or notification authority. Existing full/periodic/explicit queries and Clear Cache remain repair paths, including mutations outside the journal predicate and related effort/HR edits that a workout anchor cannot observe.

**Phase 5 lifecycle/dependent repair delivery (uncommitted):** `WorkoutChangeJournalStore.lifecycleEnabled` is a code-level `false`, not a user preference or launch argument. An explicitly injected validation file can exercise the same lifecycle. On activation or foreground-refresh completion, lazy construction/scan runs off the first-paint path and behind existing foreground work. Pending repair finishes before another journal generation is scanned. Repair takes the existing refresh slot/deadline, uses the background query pool, and processes at most three affected months per pass; unfinished work resumes on a later activation/refresh. Existing record backfill and journal repair do not occupy their repair slots concurrently. No observer, background delivery, scheduler, or disk-derived Training Load reuse was added.

**Dependent invalidation and durability:** cached month values remain visible but pending months lose validation; the dashboard freshness and watch compute-seed watermark are retired before repair. Changed workout details are invalidated behind the serial persistence barrier, with a cache epoch fence for late detail reads. Each affected month clears scoped effort reuse and runs the existing validated full month query, then writes/read-verifies its month snapshot and record fold before checkpointing. Unknown deletions/bootstrap include the retained 408-day dependency window and cached months, invalidate all rebuildable workout details, and reset the derived record ledger for its existing all-history baseline rebuild; user workout/month history is not stripped. Records remain unavailable until that baseline completes. Once months and records are repaired, the existing progressive dashboard pipeline fetches a full trend window (not the short-window merge), recomputes dependent state and its watch seed, and requires a settled freshness stamp plus durable main/sidecar and record saves before acknowledging. A failed or cancelled query, persistence failure, context switch, or abandoned refresh retains the journal obligation.

**Repair checkpoint persistence:** the artifact is the completed-month set plus detail/record invalidation progress in the journal envelope. Its invalidation key is journal generation/revision and dependency context (source scope, permissions, calendar/timezone, and repair-policy version). A new delta or restart drops progress; a changed context rebuilds it. It deliberately survives midnight for completed historical months, while final dashboard freshness retains its day/layout checks. The repair path is retrying the next bounded pass or Clear Cache; no reinstall is required. An incorrect checkpoint could skip a stale month or record repair and ultimately taint readiness, so it is written only after durable dependent state and is never itself freshness authority. Costs include readback of month/record files for unambiguous durability, one bounded journal rewrite per checkpoint, and a full query-derived dashboard pass after repairs. These are correctness costs, not measured performance improvements.

**Lifecycle verification and self-review corrections (2026-09-05):** the initial focused journal suite passed 17 tests; the follow-up lifecycle/documentation selection passed 8, including a real fake-store test of successful empty-month repair, the three-month limit, durable month files, and checkpoint relaunch. Additional coverage checks detail invalidation scope, failed month/checkpoint writes, and invalidated admission. The first full run passed 2,322 with 1 skip but failed two source guards still counting four detail-cache clear gates. Those guards now explicitly name and verify the fifth, epoch-fenced journal gate; behavior assertions were not removed. Final self-review also closed a detail-hydration race: new opens while the disk invalidation is queued now use the existing session-long live-read gate, while the cache epoch rejects older loads. This can increase cold-session detail reads after a delta; it does not weaken the existing permission-resume gate. The month durability receipt also verifies month/year as well as payload/validation, and Settings disk-size accounting includes the journal. Final acceptance passed: generic iOS Body build, **2,325 BodyTests with 1 skip and no failures** (`/private/tmp/body-rp03-p5d-full-final.xcresult`), and **85 watch tests with 1 skip and no failures** (`/private/tmp/body-rp03-p5d-watch.xcresult`). The watch-shared seam was unchanged by the later app-only detail corrections. `git diff --check` is clean; build 8, version files, and schemes are unchanged. The project-specific `karpathy-guidelines` skill kept this scoped to Phase 5 invalidation/durability and its tests/docs. No commits, physical-device installation, or Health-record mutations were performed. No remaining code wiring is deferred to device access; only physical acceptance, measurement completion, and the production activation decision remain.

**Bounds and costs:** the gated owner limits each canonical/staging generation to 10,000 entries, each anchor to 1 MiB, and each scan to at most 16 requested pages (default four) within 120 seconds. The dependent repair pass separately uses the existing 120-second refresh deadline and a three-month limit. Capacity overflow refuses the page without advancing its anchor; it never prunes existing user history. The initial lower bound stays fixed, so an exceptionally dense/long-lived journal can reach the cap; full queries remain authoritative and Clear Cache starts a new bounded bootstrap. Dirty-interval overflow collapses to full repair; pathological month coverage over 2,400 months is refused without dropping its obligation. These are safety bounds, not measured throughput tuning. Atomic page saves encode off the main actor; bootstrap temporarily retains both generations. Retention/capacity behavior and repair latency still require physical-database validation before activation. No query-pool retuning or extra telemetry was added.

**Automated gates so far:** transaction focused tests and Body build passed; its full suite passed 2,309 with 1 skip (`/private/tmp/body-rp03-p5a-full.xcresult`). The anchored seam passed Body build, full suite 2,312 with 1 skip (`/private/tmp/body-rp03-p5b-full.xcresult`), and watch 85 with 1 skip (`/private/tmp/body-rp03-p5b-watch.xcresult`). Reconciliation focused tests cover paginated relaunch, deletion, stale acknowledgments, timeout/overlap, invalid anchors, clear/context rejection of late success, and failed page/acknowledgment writes. Final infrastructure gates passed: 11 focused journal tests; generic iOS Body build; **2,317 app tests with 1 skip and no failures** (`/private/tmp/body-rp03-p5c-full.xcresult`); **85 watch tests with 1 skip and no failures** (`/private/tmp/body-rp03-p5c-watch.xcresult`). `git diff --check` is clean. This verifies the dormant infrastructure, not production Phase 5 activation. The project-specific surgical-change skill kept existing lifecycle, live consumers, versions, schemes, and UI strings untouched. No device Health records were added/deleted, no app was installed for these code changes, and no commits were created.

**Warm-resume capture limitation (2026-09-05):** `/private/tmp/body-rp03-ipad-warm-resumes-1.trace` saved automatically. Its exported logs contain one full profile (10.144 s, 249 effort candidates) and two `total=n/a` profiles (22 leaves and 1 leaf, both zero effort candidates). The exported interval table did not supply matching Body `WorkoutMonths` durations. These observations do not establish three clean, timed workout-only resumes, so no warm-resume median is claimed. Retain this trace as diagnostic evidence, not a completed baseline set. The user's subsequent sequencing amendment permits implementation while this and cold-launch/device mutation evidence remain pending.

**Sequencing amendment (2026-09-05):** after the coordinated pulls and attempted warm-resume capture, the user explicitly asked to start implementation without waiting for further device interactions. Proceed with journal schema/transaction durability, the anchored query seam, and deterministic reconciliation tests now. Existing query-derived production paths remain authoritative; production delta reuse stays disabled until physical mutation/relaunch verification passes. Available iPad runs are a partial comparison reference, not a completed cold-launch/resume/first-usable baseline. Missing device evidence remains an acceptance limitation, not a reason to fabricate measurements or enable unverified cache trust. Phases 6/7 remain deferred; no commits or Health-record mutations are authorized.

**Coordinated iPad Home pulls (2026-09-05):** the user confirmed setup and completed three pulls without requested configuration changes. Saved trace `/private/tmp/body-rp03-ipad-home-pulls-2.trace` contains three completed DEBUG profiles: 10.317 s, 10.031 s, 10.848 s (median 10.317 s; range 10.031–10.848 s). Each reports 44 leaves, 249 effort candidates, peak HK queries 11, and pool peaks interactive 10/background 1. Full per-leaf logs remain in the trace and its local `os-log` XML export. Only the latter two `RefreshRecentMonths` intervals appear in the exported interval table; the first duration is supported by its completed profile, not a complete interval capture. Launch-to-usable and visual paint were not measured by these profiles. Cold-launch and warm-resume sets remain outstanding, so the full baseline gate is not yet met. An earlier interrupted trace failed to finalize and was discarded as measurement evidence; the user repeated the pulls, and this recording ended and saved automatically. No instrumentation or app code changed.

**iPad measurement preflight (2026-09-05):** iPadPro is wired, paired, running iPadOS 26.6.1 (23G83), with Developer Mode enabled and Body 1.1.0 build 8 installed. Instruments successfully launched the installed app and captured its existing signposts and DEBUG `RefreshProfile` table in `/private/tmp/body-rp03-ipad-baseline-preflight-2.trace`. The 30-second preflight recorded `DashboardSnapshotLoad` 62.58 ms, `DaySamplesSidecarLoad` 32.27 ms, `RefreshRecentMonths` 11.649 s, peak HK queries 11, pools interactive 10/background 1, and one effort candidate. These are **uncontrolled preflight observations**, not one of the accepted three-run baseline sets: installed binary provenance, consistent settings/database and settled backfill state, launch-to-usable, and the user-triggered pull/resume runs still need confirmation. No journal code or Health-record edits were made. Proceed with coordinated device interactions before treating the baseline prerequisite as satisfied.

**Reference-device amendment approved (2026-09-05):** the user approved iPadPro (iPad Pro 13-inch M4, iPad16,5) as the Phase 5 reference device instead of waiting for either iPhone. Establish a new §6 baseline and compare before/after on this same iPad, Health database, configuration, layout, source selections, permissions, timezone, and settled history state. iPad measurements are not comparable to prior iPhone timings and must not support an iPhone performance claim. Physical anchored-query mutation, interruption, and relaunch checks may use this iPad; the evidence requirements themselves are unchanged. This supersedes the phone-only prerequisite below, not the requirement to measure before implementing. No existing Health records may be modified merely to manufacture test evidence, and commits remain unauthorized.

**Current prerequisite check (2026-09-05):** the user committed Phase 4b–4d and its review corrections in `897d0de`, `9c5c432`, `76e4357`, and `6ab8dfa`; the working tree was clean at entry. Phase 4's automated gates passed as recorded above. A fresh read-only `devicectl list devices` still reports iPhoneAir and iPhonePro unavailable, with only iPadPro connected. The reference-phone §6 baseline and physical mutation/relaunch evidence remain outstanding. Phase 5 is therefore blocked at its approved prerequisite gate; no journal code, device installation, or Health database mutation was performed. Continuing with an isolated non-production prototype would require the explicit sequencing amendment described below. Phases 6 and 7 remain deferred, and no commit is authorized.

**Prior Phase 5 prerequisite check (2026-09-04, before the instruction to finish Phase 4):** the working tree is clean at `51f2396`, following `fd350c7` for Phase 4a. Phase 4b remains paused over the effort-failure propagation correction; 4c and 4d are unstarted. Current source still excludes cached effort IDs without a validation timestamp (`HealthKitFetchEngine.swift:2756–2761`) and rejects the entire full-window trend result on any leaf failure (`HealthKitWorkoutStore.swift:3330`). Workout anchors do not repair related effort mutations, so enabling a journal is not a substitute for those prerequisites. A fresh read-only device inventory again reports both iPhones unavailable; the connected iPad is not the established reference phone/database, and no comparable baseline or physical mutation/relaunch proof has been captured. No Phase 5 code was written during this gate check.

**Sequencing decision approved:** finish Phase 4b–4d first, including the documented 4b failure/freshness propagation correction, then return to Phase 5 with the reference phone available for §6 and mutation verification. Starting an isolated, non-production journal prototype before those prerequisites would be a different sequence requiring an explicit amendment; it would not authorize trusting disk deltas in production or count as completed Phase 5.

**Work:** limit the first journal to workouts; implement schema/transaction durability, the anchored fake-store seam, and reconciliation as separate commits. Commit deltas and their anchor together, preserve explicit/periodic repair, and prove deletion, bootstrap, interruption, and invalid-anchor recovery. Do not assume workout anchors detect effort-relationship edits.

**Acceptance:** deterministic transaction tests plus physical-device mutation/relaunch evidence. No broad quantity/sleep rollout without reviewed dependency mapping. If a comparable device baseline or mutation proof is unavailable, report the gate as blocked rather than claiming delta reuse is safe.

### Implementation Phase 6: observer-driven and background work (P9)

**User decision (2026-09-04): deferred for now.** Do not implement observer delivery or new background scheduling in this pass. Existing resume/manual refresh and background-warning behavior remain unchanged. Revisit only on a later explicit request.

**Depends on:** Phase 5's durable dirty protocol and Phase 3's warning safety.

**Work:** first connect observer changes to coalesced foreground dirty work; then add background delivery/scheduler integration in a separate commit. Persist unfinished obligations, acknowledge callbacks promptly, and keep data freshness independent of notification enablement. Background execution is opportunistic, not a freshness guarantee.

**Acceptance:** repeated callbacks coalesce, foreground work retains priority, interruption leaves repair pending, and disabled notifications do not disable data repair. Verify simulated callbacks and physical-device delivery with the standard per-commit gates.

### Implementation Phase 7: measured disk-derived compute reuse (conditional P8)

**User decision (2026-09-04): deferred for now.** Do not switch Training Load or other derived computation to persisted canonical inputs in this pass. Existing query-derived computation remains in place. Revisit only on a later explicit request with the required parity and measurement evidence.

**Depends on:** Phase 5; repeat §6 measurements after the intervening changes.

**Work:** only proceed if canonical persisted training-load inputs have demonstrated query parity and useful measured benefit. Specify the artifact's invalidation key, repair path, and stale-value consequence before introducing it. Compare bootstrap, clean reuse, and mutation repair separately.

**Acceptance:** unchanged calculator results and watch parity, reliable invalidation/repair, and a recorded benefit on the same device/database baseline. If the evidence does not justify persistence, close this phase with a documented no-change decision. No budget/window retuning or speculative speed estimates.

### Execution record

- Phase 1a: implemented and verified on 2026-09-04. `HealthKitWorkoutStore` now captures canonical source/settings identity and an independent monotonic revision, carries it through deadline-guarded refreshes, observes store-owned changes before their first suspension, and rejects incompatible detached dashboard/permission/readiness/stress/Radar results and late Training Load seed updates. The queued watch-send callback checks the captured inputs as well as the cache epoch. Four new `HealthKitRefreshInputContextTests` cover stable identity, cosmetic source naming, cancelled A→B→A edits, and a scripted HealthKit read followed by a goal change before freshness admission. README, TestPlan, and a dedicated documentation guard are synchronized. No formulas, query budgets, deadline limits, versions, or UI strings changed.
- Phase 1a verification: generic iOS Body build passed; focused context/deadline run passed 15 tests; full BodyTests single-worker run passed 2,207 tests with 1 skip; BodyWatchTests passed 85 tests with 1 skip (unsigned-host App Group access). The initial parallel BodyTests run had 2,206 passes, 1 skip, and one existing publisher-test five-second expectation timeout alongside simulator worker launch errors. The publisher/context/doc retry passed all 9 tests before the clean full single-worker run. No test timeout or assertion was weakened. Results: `/private/tmp/body-rp03-context-full-serial.xcresult` and `/private/tmp/body-rp03-watch-context.xcresult`.
- Phase 1b: implemented and verified locally, **not committed**. Adds per-kind summary/daily/raw compatibility, resolved membership provenance, nested sleep-vital invalidation, schema 3 sidecar manifests, queued publication admission, engine query revision checks, and one coalesced corrective owner. Goal-only correction uses cached inputs. New regression coverage exercises failed-source fallback and companion input captures, persistence reload, detached compute rejection, and coalescing. Existing source-discovery expiry, authoritative-empty mutation repair, progress/freshness migration, and calendar rollover scheduling remain in their later phases. No query budgets, deadline limits, formulas, or UI strings changed. The separately authorized build bump sets all twelve target/configuration entries to build 8, with marketing version 1.1.0 unchanged.
- Phase 1b passing gates: generic iOS Body build passed. Focused guard/cancellation/source coverage passed 171 tests (`/private/tmp/body-rp03-p1b-guards.xcresult`). The final focused confirmation passed all 13 new cache-scope tests plus the existing cancellation test (`/private/tmp/body-rp03-p1b-isolated-confirmation.xcresult`). BodyWatchTests passed 85 tests with 1 skip (`/private/tmp/body-rp03-p1b-watch-final.xcresult`). README, TestPlan, and documentation guards are synchronized.
- Earlier acceptance blocker, now resolved: full single-worker BodyTests runs repeatedly reported 2,219 passes, 1 skip, and a crash while `HealthKitLeafFailureSemanticsTests.testDiscoverSourcesFailsSilentlyOnCancellation` was running, including with retry enabled (`/private/tmp/body-rp03-p1b-final-repeated.xcresult`). The runtime/XCTest stack alone did not identify the cause. Running without concurrent build jobs did not eliminate it; a temporary nonthrowing cancellation-test experiment also reproduced it and was fully reverted. The runner's named test was misleading: the exported exception identifies the retained coalescing fixture described below. No test was skipped, assertion weakened, or timeout increased to obtain a passing gate.
- User review corrections (2026-09-04): rejected commits in both refresh bodies now throw into the existing cleanup tail. Cleanup checks deadline-generation ownership rather than settings-input admission, because a changed source must invalidate the result without stranding its anchor or memoized training-load fetch. An abandoned body still cannot clear a newer anchor. Explicit source/group/combine/sleep-parsing actions preserve `.userInitiated` through coalescing; passive observations cannot downgrade a queued explicit action, and a goal-only edit still computes from cached inputs. The user-requested dead `truncatePersistedDaySamples()` helper and its stale three-full-strip comment were removed together; the sidecar guard now describes scoped persistence. Three additional integration tests cover both cleanup paths, newer-anchor protection, and the seven settings actions.
- Full-suite crash investigation correction: exported runner diagnostics identify `NSInternalInconsistencyException: API violation - multiple calls made to -[XCTestExpectation fulfill] for latest context refresh` immediately before the runtime abort. That expectation belongs to the new `testSourceEditsCoalesceToLatestContext`, not the cancellation test named by the runner. Its stored override strongly captured its own store, retaining the fixture and entitlement observer into later tests. The hook now captures the store weakly and is cleared when the test exits; over-fulfillment checking remains enabled. The existing cancellation test and its timeout are unchanged. Two consecutive full-suite runs now pass, resolving the acceptance blocker.
- Final Phase 1 acceptance on build 8: generic iOS Body build passed with `CODE_SIGNING_ALLOWED=NO`; both complete single-worker BodyTests runs passed **2,223 tests with 1 skip and no failures** (`/private/tmp/body-rp03-review-fixes-full.xcresult`, `/private/tmp/body-rp03-review-fixes-full-confirmation.xcresult`); BodyWatchTests passed **85 tests with 1 skip** (`/private/tmp/body-rp03-review-fixes-watch.xcresult`). The new anchor/intent cases passed in the initial focused run; its three stale source/document guard failures were corrected before both full passes. Built phone and watch bundles report build 8; the phone still reports marketing version 1.1.0. `git diff --check` is clean. No new commit was created. A separate `BodyWatchTests.xcscheme` ProfileAction edit appeared during this work and was left untouched.
- Phase 1 was subsequently committed by the user: `d7cc12a` (input fence), `c489f22` (scoped fallback), and `26fd85f` (build 8 preparation and follow-up fixes). Earlier uncommitted notes above record the verification state at that time.
- Phase 2: implemented and verified locally, **not committed**. Envelope version 1 binds metadata to the exact dashboard payload in one atomic replacement (rather than a separate revision key). Input revision admission remains the captured publication token from Phase 1. The nine new `HealthDashboardDurabilityTests` cover independent write/encode failures, both partial-write directions and retries, reload boundaries, migration, dedupe, live versus durable freshness, and ring progress. Existing ring/TTL tests now assert the live-versus-durable contract; a source guard requires all ten saves to carry metadata. README, TestPlan, and documentation guards are synchronized.
- Phase 2 acceptance: final generic iOS Body build passed with `CODE_SIGNING_ALLOWED=NO`; complete single-worker BodyTests passed **2,238 tests with 1 skip and no failures** (`/private/tmp/body-rp03-p2-full-2.xcresult`); BodyWatchTests passed **85 tests with 1 skip** (`/private/tmp/body-rp03-p2-watch.xcresult`). The initial focused run passed 248 tests and exposed one source guard still naming the old save API. The first full run passed 2,237 tests with 1 skip and exposed one test still expecting the intentionally retired UserDefaults freshness write. Both assertions were updated to the new contract before the final passing suite; no crash, timeout adjustment, or assertion weakening was needed. `git diff --check` is clean. Build/version files and the watch ProfileAction remain untouched, and no commit was created.
- Phase 2 was subsequently committed by the user. The user retained the conservative legacy migration after review: unbound defaults completion is not imported as durable coverage proof.
- Phase 3 initial handoff, before the ring amendment approval: P7 and P4 Body builds passed; the source-discovery full suite passed **2,250 tests with 1 skip** (`/private/tmp/body-rp03-p3-source-full-2.xcresult`) and its watch gate passed **85 tests with 1 skip** (`/private/tmp/body-rp03-p3-source-watch.xcresult`). The first source-discovery full run exposed one stale source guard expecting the pre-force call signature; the guard now requires explicit repair and intent-based force call sites. P5 focused calendar/admission tests passed, followed by full BodyTests **2,258 passed with 1 skip and no failures** (`/private/tmp/body-rp03-p3-calendar-full.xcresult`). Generic iOS Body build passed and BodyWatchTests passed **85 with 1 skip** (`/private/tmp/body-rp03-p3-calendar-watch.xcresult`). P5 was explicitly left incomplete pending review of the ring-history identity amendment, which the user subsequently approved. No test assertion was weakened and no commit was created.
- Current continuation: Phase 4a is committed; 4b–4d are implemented, uncommitted, with passing automated gates. Phase 4's remaining device mutation checks are not claimed complete. Phase 5 waits for Phase 4 and its device evidence; Phases 6–7 are explicitly deferred.

#### Phase 3 final acceptance after approved ring amendment

- Implemented and verified locally, **not committed**, on 1.1.0 build 8. The approved migration adds explicit ring day identities and resume-day metadata, preserves unproven records in an archive, and reconciles them only against successful coverage. A successful empty chunk replaces known data only within that chunk's exact validated window. Remaining archive repair is bounded to three months per pagination pass. The ring detail rebuilds its display dates on an active time-zone change. No workout ledger, frozen-observation reset policy, version setting, scheme, query budget, or deadline was changed.
- Focused migration and existing ring/durability coverage passed **292 tests** (`/private/tmp/body-rp03-ring-identity-focused-2.xcresult`). The first final full-suite attempt compiled but the simulator refused app launch during preflight, before tests ran (`/private/tmp/body-rp03-phase3-final-full.xcresult`). The unchanged retry passed **2,266 tests with 1 skip**. After adding the exact-window authoritative-empty regression, the final full BodyTests passed **2,267 tests with 1 skip and no failures** (`/private/tmp/body-rp03-phase3-final-full-3.xcresult`). No test was disabled or timeout increased.
- Final generic iOS Body build passed with `CODE_SIGNING_ALLOWED=NO`; BodyWatchTests passed **85 tests with 1 skip** (`/private/tmp/body-rp03-phase3-final-watch.xcresult`). README, TestPlan, and documentation guards are synchronized; `git diff --check` is clean. Project-specific surgical-change guidelines kept changes within the approved phase and its tests/docs. Physical-device timezone travel, HealthKit comparison, and process-kill checks remain explicitly manual in TestPlan, not claimed as simulator observations.

#### Phase 3 pagination review corrections

The user's four follow-up findings were verified against the production caller and repair loop. Both the detail-view load admission and loading indicator now use the same archive-aware predicate, so a known history start does not hide pending repair. The store computes pending repair keys once per attempt and the model shares one UTC attribution calendar; this is allocation removal, not a measured performance claim. Archive repair retains the history-end decision and existing exhausted-month set, and bypasses ordinary pagination's gap accumulator entirely. Empty repair months contribute successful validation coverage, not unrelated visible loaded-month gaps. Regression tests cover admission after history exhaustion, successive scattered empty/nonempty repairs, preserved probes, failure/epoch rejection, and both view predicate call sites. The detail's calendar also reads the current zone so its P5 time-zone notification cannot reuse a captured old calendar. README, TestPlan, and documentation guards are synchronized. Focused coverage passed **66 tests** (`/private/tmp/body-rp03-pagination-review-focused.xcresult`); final full BodyTests passed **2,269 tests with 1 skip and no failures** (`/private/tmp/body-rp03-pagination-review-full.xcresult`); generic iOS Body build passed; BodyWatchTests passed **85 tests with 1 skip** (`/private/tmp/body-rp03-pagination-review-watch.xcresult`). `git diff --check` is clean. Changes stayed within the P5 review scope; build 8, version guards, and schemes are unchanged. No commits were created.

#### Phase 3 nonblocking review follow-up

Both findings were approved for implementation. The store now derives `pendingActivityRingRepairMonthKeys` on every ring-history assignment and explicitly seeds it during initialization. Both scroll admission and the bounded pagination attempt reuse this observable in-memory array. Its invalidation identity is the current history value (including archived days and validated months); it is never persisted, and reload or any replacement rebuilds it. A wrong queue could delay archive repair or schedule an unnecessary read, but cannot bypass the existing epoch, permission, coverage, or merge guards. No new disk artifact, schema, query, or telemetry was added.

Before changing production code, a same-simulator fixture of 3,650 legacy days measured three batches of 30 reads after `hasMoreActivityRingHistory` became false: **240.684, 236.170, 234.704 ms**. After caching: **0.008791, 0.005334, 0.004667 ms**. Bundles: `/private/tmp/body-rp03-followup-baseline.xcresult` and `/private/tmp/body-rp03-followup-focused.xcresult`. These are isolated Debug admission-read measurements, not launch/refresh or scrolling measurements; the scan cost moves to history assignment and memory retains one month-key array. RP-02 §6's three cold/full/warm runs on the same real Health DB, existing refresh/per-leaf signposts, and launch-to-usable checks remain required before claiming an end-to-end improvement. No real-device baseline was collected here.

The warning evaluator's mid-loop admission exit now retains observed failures, returning `.failure` or `.partialFailure` according to whether any notification was posted; without an earlier failure it still returns `.skipped`. The scheduler mapping, eligibility checks, and ledger behavior are unchanged. Tests cover all four earlier-failure/delivery combinations, rejected final submission, and successful-only ledger updates. Ring regressions cover initial cache seeding, partial/full coverage, newly merged archive data, and repeated admission reads. README, TestPlan, and documentation guards are synchronized. Focused tests passed **41 tests with no skips or failures**. Full acceptance gates are pending. No commits were created.

Acceptance follow-up: the first full-suite attempt failed simulator launch preflight (`Busy`), before test execution. The retry ran **2,271 passed, 1 skipped, 1 failed**: `testZoneChangeBeforeComputeAdmissionRejectsOldBuckets` lost its frozen record. Its fixture used the literal `original frozen context`, which differs from the store's current readiness signature and triggers `rebuildStaleReadinessOverlay` during initialization. That independent async repair can legitimately clear the fixture before the calendar assertion; isolation passed, showing the scheduling dependence. The calendar-store fixture now supplies the real signature matching its injected selections and current sleep preferences. No assertion or production invalidation policy was changed. Generic iOS build passed; watch tests passed **85 with 1 skip** (`/private/tmp/body-rp03-followup-watch.xcresult`). Final full-suite acceptance remains pending after the fixture correction.

Final follow-up acceptance: corrected calendar tests passed **6/6** (`/private/tmp/body-rp03-followup-calendar-corrected.xcresult`). A second full-suite launch attempt hit the same preflight-only simulator error; its retry passed **2,272 BodyTests, 1 skipped, 0 failures** (`/private/tmp/body-rp03-followup-final-full-retry.xcresult`). Together with the passed generic iOS build and **85 passed, 1 skipped** watch gate, this completes verification for both nonblocking fixes and the test-only fixture correction. Build 8, version guards, and schemes remain unchanged; `git diff --check` passes. No commits were created.

#### Follow-up review: permission transaction ordering and scheme restoration

- Confirmed failure: `updateHealthPermission` scheduled its correction before the awaited privacy strip and `applyPermissionSelectionToCachedData` finished. A correction starting after the 300 ms debounce could publish under the same settings identity, then be overwritten by the filter's older captured snapshot. The existing input/scope guards could not detect that same-context race.
- Correction: permission mutations synchronously register pending cleanup before input capture. The corrective owner checks for pending permission work both before scheduling and after acquiring a free slot. Each cleanup waits for the old refresh, owns the refresh slot through disk sanitization, filtering, and publication, then releases it. Only after all pending permission transactions finish can one latest-context correction run. An already-applied permission change completes cleanup in an owned task even if its Settings caller is cancelled; a delayed transaction mirrors the latest permission selection into the engine. This preserves immediate input invalidation while serializing the mutation's repair. Cost: corrective fetching waits for privacy cleanup instead of overlapping it. No new persisted artifact, query budget, timeout, or build increment.
- Verification added: paused disk-strip and filter cases held beyond the debounce, a pre-armed corrective owner with a scripted fresh HealthKit sample, overlapping permission toggles, and cancellation while waiting for a busy refresh slot. The fresh corrective result must survive the completed toggle and reach companion inputs. Focused, full app, and watch gates are required again for this correction.
- Restored the exact nine-line ProfileAction `MacroExpansion` from `78f60f4` in `BodyWatchTests.xcscheme`, as requested. The scheme now has no working-tree diff; the earlier execution record's decision to leave that edit untouched is superseded.
- Follow-up acceptance passed on build 8: focused context/cache/source guards **164 passed** (`/private/tmp/body-rp03-permission-serialization-focused.xcresult`); full BodyTests **2,227 passed, 1 skipped, no failures** (`/private/tmp/body-rp03-permission-serialization-full.xcresult`); BodyWatchTests **85 passed, 1 skipped** (`/private/tmp/body-rp03-permission-serialization-watch.xcresult`); generic iOS Body build passed with `CODE_SIGNING_ALLOWED=NO`. README, TestPlan, and their documentation guards are synchronized. The restored scheme still has no diff after all gates, and `git diff --check` passes. No commit or additional version change was made.
